# ESTADO DO PROJETO — Heian Tour

> **Última atualização:** 2026-08-28 · Antigravity
> Fonte única de verdade entre Claude e Antigravity. Leia UMA vez no início da sessão. Não releia a cada comando. Ao terminar, acrescente **uma linha** no Log.

---

## Log (mais recente no topo — mantenha só as ~15 últimas linhas)
- 2026-08-29 · Antigravity · **Integração das Fotos Personalizadas nos Cards de Compras (`cliente.html`, `/public/img/guia/`).** Salvas e vinculadas as 4 fotos oficiais fornecidas pelo usuário para os cards do Guia de Compras: (1) Bolinhos Tokyo Banana Matcha em Comida & Souvenirs, (2) Produtos e cosméticos J-Beauty (Fino/Shiseido) em Cosméticos & Beleza, (3) Facas artesanais de aço damasco em Cultura & Souvenirs e (4) Tênis Onitsuka Tiger Mexico 66 em Moda & Streetwear. Enquadramento e proporções padronizadas. Validação OK. Só frontend → Ctrl+Shift+R.
- 2026-08-29 · Antigravity · **Curadoria Precisa de Imagens nos 6 Cards de Compras (`cliente.html`).** Substituídas as fotos genéricas por imagens fiéis ao tema e às marcas de cada bloco: (1) Megastores e ruas de compras de Tokyo para as Musts, (2) Doces finos, wagashi e lembranças de Depachika para Comida & Souvenirs, (3) Frascos de skincare/sérum para Cosméticos & Beleza, (4) Câmeras profissionais e lentes para Eletrônicos (Yodobashi/Bic), (5) Facas de corte artesanais feitas à mão para Cultura & Souvenirs e (6) Tênis casuais retrô clássicos para Moda & Streetwear. Validação OK. Só frontend → Ctrl+Shift+R.
- 2026-08-29 · Antigravity · **Guia Completo de Tax-Free com Regra de Nov/2026 e Dicas Heian (`cliente.html`).** Atualizado o card de Tax-Free no Guia de Compras da Área do Cliente com distinção clara entre a regra atual (até 31/10/2026 com desconto no caixa) e a nova regra oficial (a partir de 01/11/2026 com reembolso no aeroporto / fim dos sacos lacrados), além de bloco destacado de Dicas Heian sobre valor mínimo por compra e porte obrigatório do passaporte físico. Validação OK. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Trava de Card/Popover de Atração por Clique (`cliente.html`).** Implementada a trava oficial: ao clicar no chip da atração, o card fica 100% fixado na tela (`window.popoverFixed = true`), permitindo mover o mouse e clicar em 'Como Chegar' sem sumir. O card só fecha se o usuário clicar no botão '✕' ou em qualquer área fora do card. No hover rápido, adicionado delay de transição e persistência do cursor sobre o popover. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Reformulação e Disparo Direto de Google Maps em Atrações (`cliente.html`).** Atribuído `onclick` inline no gerador de chips (`criarChipAtracaoHTML`) e implementado `window.abrirMapsAtual` como `<button type="button">` com `z-index: 9999`, além de posicionamento adaptativo (modal central no mobile e tooltip inteligente no desktop), garantindo abertura infalível do Google Maps com 1 clique/toque. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Fix: Blindagem e Clique Direto do Google Maps em Atrações (`cliente.html`).** Corrigida a captura de elementos filhos de chips de atração (`e.target.closest('.chip-atracao')`) que impedia a leitura do nome/id da atração, e adicionado handler de abertura explícita com `window.open` e `stopPropagation` no botão `Como Chegar (Google Maps)`, garantindo abertura imediata no app/navegador do Google Maps. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Compatibilização Total de Chaves de Google Maps nas Atrações (`config.atracoes`, `app.js`, `roteiros.js`).** Sincronizadas todas as 172 atrações para conter o link nas propriedades `'Google Maps'`, `'Link do Google Maps'` e `'mapsUrl'`. Atualizados os modais de edição do painel e de rotas para ler e salvar de forma transparente qualquer variação da chave, garantindo exibição instantânea dos links no formulário. Validação OK. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Auditoria e Geocodificação Exata do Google Maps em 172 Atrações (`config.atracoes`, `cliente.html`, `roteiros.js`).** Mapeadas e cadastradas as URLs oficiais do Google Maps com portões principais e pontos exatos de entrada para todas as 172 atrações da Base Mestre em todas as cidades do Japão (Tokyo, Kyoto, Osaka, Nara, Hiroshima, Miyajima, Hakone, Kamakura, Kanazawa, Takayama, Shirakawa-go, Nikko, Yokohama, Naoshima, Koyasan). Testes automatizados de integridade HTTP confirmaram 100% de sucesso (172/172 links ativos e sem erros). Validação OK. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Integração do Google Maps em Atrações (`app.js`, `roteiros.js`, `cliente.html`).** Adicionado campo de URL/Link do Google Maps na Base Mestre de Atrações (admin e edição rápida de roteiros). Na Área do Cliente, implementado botão em destaque `📍 Como Chegar (Google Maps) ↗` no popover de cada atração, com fallback inteligente para pesquisa automática de rota no Japão quando o link manual não estiver preenchido. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Ativação e Teste Oficial do Programa de Afiliados Klook (`app_config`, `transportes`, `experiencias`).** Cadastrado o ID de Afiliado oficial da Heian Tour (`aid: 132901`, `aff_adid: 1404889`) em `app_config`; atualizados 308 links de transportes e experiências na Base Mestre com deep links de redirecionamento. Testes HTTP reais confirmaram redirecionamento 302 com `Set-Cookie: true` e injeção do `aff_klick_id` para atribuição de comissão de 30 dias. Validação OK. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Padronização de Preços de Atrações para `Gratuito` (`config.atracoes`, `roteiros.js`, `cliente.html`).** Normalizadas todas as variações textuais de preço ("Grátis", "grátis", "0", etc.) para o termo oficial padronizado **`Gratuito`** em todas as 172 atrações da Base Mestre, nos roteiros e nos popovers de exibição do painel e do cliente. Validação OK. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Navegação Direta para o Card com Rolagem e Foco Pulsante (`cliente.html`).** Ao clicar no botão de um transporte ou experiência na Timeline do Roteiro, a Área do Cliente troca automaticamente para a aba de Vouchers, ativa a sub-aba correta (Viajante ou Heian), rola a página de forma suave centralizando o card exato e aplica um efeito visual de foco pulsante (`cardPulseFocus` / `cardPulseFocusGreen`), eliminando a necessidade de o cliente procurar o item manualmente. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Sincronização Visual de Compra Concluída no Roteiro (`cliente.html`).** Quando o viajante marca uma tarefa como comprada no Checklist/Vouchers (ou anexa o bilhete), a Timeline Diária do Roteiro atualiza automaticamente o card para verde (`✅ Compra Concluída pelo Viajante` e `Passagem/Ingresso comprado · Ver modo de usar →`), mantendo coerência total entre a timeline e a aba de vouchers. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Estrutura Completa de Compra e Visita para Experiências (`app.js`, `cliente.html`).** Adicionados os campos `compra` (Instrução de Compra / Pré-compra) e `uso` (Instrução de Visita / Como Entrar) na Base Mestre de Experiências. Integrada a resolução automática desses dados na Área do Cliente e no Checklist do Viajante, com badges de abertura de vendas e botões de acesso a ingressos. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-28 · Antigravity · **Classificação Robusta de Transportes Urbanos + Texto Refinado de Cartão IC (`cliente.html`).** Criada a função `isTransporteSemReserva(el)` que classifica automaticamente trens urbanos, metrôs e linhas metropolitanas (ex: JR Yokosuka) como Cartão IC / Dinheiro na Catraca (azul), evitando que caiam no fluxo de compra do viajante. Atualizado o texto informativo para incluir pagamento em dinheiro nas máquinas da estação. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Blindagem de Preços para Táxi/Variável + Bump de Cache (`roteiros.js`, `app.html`).** Inserida regra prioritária que impede terminantemente a exibição de estimativas herdadas para itens de Táxi, App GO, Carro, Van ou com categoria Variável (mesmo se houver `trechoId` antigo de trem no elemento). Atualizada versão de assets para `?v=20260827_02`. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Fix: Correspondência Estrita de Preços de Transportes (`roteiros.js`, `cliente.html`).** Corrigida a busca de estimativa para exigir correspondência exata de modal/tipo de transporte. Impede que itens como Táxi / App GO herdem erroneamente o valor de trens (como Narita Express) apenas por compartilharem o mesmo trecho geográfico. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Estimativa em Linha Dedicada nos Cards (`roteiros.js`, `cliente.html`).** Aplicada a Opção 2 (linha dedicada com destaque visual) para a exibição de preços estimados (`Estimativa: ~¥... / pessoa`) em Transportes e Experiências no Preview, PDF e Área do Cliente. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Estimativa de Valores Unitários no Roteiro (`roteiros.js`, `cliente.html`).** Integrada a exibição de preços estimados por pessoa (`~¥... / pessoa`) para Transportes, Experiências e Atrações pagas, puxados automaticamente das Bases Mestres. Refletido na Pré-visualização, PDF/Impressão e Timeline da Área do Cliente. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Título Customizável do Dia com Fallback Automático (`roteiros.js`, `cliente.html`).** Adicionado campo de texto no cabeçalho de cada dia no editor (`dia.titulo`): permite ao roteirista editar livremente o título/rota do dia (ex: `Hiroshima & Bate-volta a Miyajima`). Se deixado em branco, o sistema continua gerando a rota automaticamente. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Cache-Busting nos Scripts Admin (`app.html`).** Atualizadas as tags de versão dos scripts para `?v=20260827_01` (`roteiros.js`, `app.js`) garantindo que todos os computadores (inclusive da Deborah) baixem instantaneamente os scripts atualizados sem ficarem presos no cache do navegador. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Auto-Restauração Robusta de Roteiro pós-F5 (`roteiros.js`, `app.js`).** Integrada a função `editarRoteiroCard` ao ciclo de inicialização pós-F5 (`carregarBases` e `navToPage('roteiros')`): garante a abertura direta do painel de detalhe do roteiro assim que a base termina de carregar, sem depender de cliques manuais ou cair no estado vazio. Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Tipografia: Meses e Passageiros em Minúsculo (`roteiros.js`, `cliente.html`).** Padronizados os nomes dos meses e termos de passageiros em minúsculo na capa do roteiro e no portal do cliente (ex: `29 de setembro a 19 de outubro de 2026` e `6 adultos`). Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Novo Padrão de Cabeçalho dos Dias (`roteiros.js`, `cliente.html`).** Alterada a ordem de exibição no topo dos cards diários para o formato: `29/09 (Dia 1) — Aeroporto de Narita → Tokyo (Hotel)`, padronizando a data e o dia no início em toda a visualização do roteiro (PDF, Preview e Área do Cliente). Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Fix: Data de Início no Preview e Capa do Roteiro (`roteiros.js`).** Corrigido o cálculo do período na Capa do PDF / Pré-visualização e no subtítulo do editor: agora prioriza `cliente.dataInicio` e as datas reais dos dias do roteiro em vez de usar `cliente.dataOrcamento` (que apontava a data de criação/hoje). Validação `node --check` OK. Pacotes de deploy regenerados. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Pacote de deploy gerado e verificado (`heian-tour-DEPLOY.zip` / `heian-tour-prod.zip`).** Pacotes oficiais de deploy criados com 63 arquivos, 0 barras invertidas, sem `.env` e validação de sintaxe JS aprovada em todos os arquivos (`server.js` + `public/js/*.js`). Inclui todas as melhorias de Transportes, Vouchers, Checklist, sincronização de cotação e correção da Área do Cliente. ⚠️ Como leva `server.js`, reiniciar o Node após extrair na Hostinger.
- 2026-08-27 · Antigravity · **Harmonização do Card do Roteiro Heian (`cliente.html`).** Corrigido o card da Timeline quando o transporte é gerenciado pela Heian: agora exibe a caixa verde consultiva *"🛡️ Reserva inclusa no pacote · Bilhete oficial disponibilizado próximo à data da viagem"* com atalho para a aba de Vouchers, em vez de exibir erroneamente as instruções de compra por conta própria. Validação de sintaxe JS OK. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Ajuste de redação dos Vouchers Heian (`cliente.html`).** Padronizada a mensagem consultiva na aba Documentos & Vouchers Heian para: *"Reserva inclusa no pacote · Bilhete oficial disponibilizado próximo à data da viagem"*, em harmonia com o card do roteiro diário. Validação de sintaxe JS OK. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **UX de Vouchers & Checklist no Portal (`cliente.html`).** Refinamentos solicitados pelo Diogo: (1) **Roteiro Diário limpo e despoluído:** Removida a Dica de Ouro dos cards da Timeline diária; (2) **Dica de Ouro nos Vouchers:** Exibida na aba Vouchers Heian e nos cards do Checklist; (3) **Transição Inteligente no Checklist:** Ao marcar a tarefa como comprada/concluída, o box laranja de *Como Comprar* se transforma automaticamente no box verde de *Instruções de Embarque (Modo de Usar)* passo a passo (`dbT.uso`). Validação de sintaxe JS OK. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Fase 1: Redação e distinção visual de Transportes & Experiências na Área do Cliente (`cliente.html`).** Reformulada a apresentação nos cards do dia e na aba Vouchers: (1) **Cartão IC (Sem Reserva)** com badge azul e aviso claro de aproximação direta na catraca; (2) **Compra pelo Viajante** com badge laranja e botão direto para o Checklist de Compras; (3) **Incluso pela Heian** com badge verde, mensagem consultiva de acompanhamento (sem o texto rígido de 30 dias) e atalho direto para o bilhete emitido. Backup `_backups/cliente.html.bak-fase1-transportes-20260827`; validação de sintaxe JS OK. Só frontend → recarregar (Ctrl+Shift+R).
- 2026-08-26 · Claude · **Modais não fecham mais ao clicar fora (`app.js`).** A pedido do Diogo: clicar no fundo descartava tudo que já tinha sido preenchido (Cadastrar Nova Atração, transporte, experiência, edição de atração etc.). Causa única: `openModal()` (app.js:2105) registrava `modalOverlay.onclick` que fechava ao clicar no backdrop. Removido (agora `onclick=null`). Os modais compartilhados (via `modalOverlay`/`openModal`) só fecham por **X (`#modalClose`), Cancelar ou Salvar** — todos têm esses. O outro sistema (`modalEditarElementoRoteiroRapido`) já não fechava no backdrop. Sem ESC-close (o keydown global é só Ctrl+Z). Bump `app.js?v=20260826_01`. Backup `app.js.bak-modalclose-20260826`; `node --check` OK. **Verificado ao vivo: modal aberto + campo preenchido + 2 cliques no fundo → continua aberto e preserva o texto; X fecha; Cancelar/Salvar fecham.** Só frontend → Ctrl+Shift+R.
- 2026-08-26 · Claude · **Atalhos na lateral direita (aparecem ao rolar) + barra de cima deixa de ser fixa (`builder-enhance.js`, `app.html`).** A pedido do Diogo (mockup aprovado): a barra grande do topo (`#roteiroEditHeaderDisplay`) tinha `position:sticky` e comia espaço permanente — trocada p/ `position:relative` (rola embora). Em troca, `renderSummary()` (no `builder-enhance.js`, que monta o painel `#rbSummary` — já sticky) ganhou um painel **⚡ Ações rápidas** com 5 botões: Salvar Roteiro (destaque), Pré-visualizar, Desfazer | Perfil (2 col), Voltar para Clientes. Cada botão só faz `.click()` no botão original do header (via `window.__rbQA(id)`) → comportamento idêntico, zero risco. Revelação por `IntersectionObserver` no header: some no topo (barra visível), aparece+fade quando a barra sai da tela, e gruda junto com o `#rbSummary` por todos os dias. CSS injetado (`#rbQAStyles`). Bump `builder-enhance.js?v=20260826_01`; `app.html` sem sticky. **Verificado ao vivo (localhost, roteiro de 38 dias): topo→barra+sem atalhos; rola→barra some, atalhos aparecem e grudam até o Dia 14+; volta ao topo→atalhos somem, barra volta.** Backup `builder-enhance.js.bak-atalhos-20260826`; `node --check` OK. Só frontend → Ctrl+Shift+R.
- 2026-08-26 · Claude · **FIX CRÍTICO: botão 'Editar atração' não clicava (pointer-events) — reproduzido e corrigido no app ao vivo.** O `.atracao-popover` tem `pointer-events:none` no CSS (herança da época em que era tooltip de HOVER), então o BOTÃO herdava `none` e o clique de mouse REAL atravessava o botão e caía no chip atrás — por isso 'nada acontece'. Enganava porque `btn.click()` por código dispara o handler direto (ignora hit-testing), então nos testes por JS parecia funcionar. Diagnóstico ao vivo (Chrome no localhost:3000): `elementFromPoint(centro do botão)` retornava um `.chip-seq`, não o botão; `getComputedStyle(pop).pointerEvents==='none'`. **Fix:** `showPopover` reseta `pointerEvents='none'` (seguro p/ hover dos chips de preview) e `__openChipPopover` (caminho do CLIQUE) liga `pointerEvents='auto'` no popover → botão herda `auto`. CSS é `none` SEM `!important`, então o inline vence. Verificado ao vivo pós-fix: `pop_pe/btn_pe='auto'` e `elementFromPoint(botão)===botão` (true) — mesmo hit-testing do clique real. Bump `roteiros.js?v=20260826_07`. Backup `roteiros.js.bak-pointerevents-20260826`; `node --check` OK. Lição: testar interações de clique com hit-testing real (`elementFromPoint`/clique de mouse), não só `.click()`. Só frontend → Ctrl+Shift+R.
- 2026-08-27 · Antigravity · **Fix: Roteiro ativo não refletia na Área do Cliente (`server.js`).** O `getClientDataHelper` usava um `roteiros.find()` ingênuo que não descartava `deletado: true` nem ordenava por recência, capturando roteiros antigos/deletados em vez do roteiro em edição ativa (ex.: Gomes & Clos recebia roteiro teste de 31/07 em vez dos 21 dias ativos). Aplicada filtragem `!r.data.deletado` e ordenação por recência/conteúdo consistente com as cotações. Backup `_backups/server.js.bak-roteiroclient-20260827`; `node --check` OK; teste real validado. ⚠️ muda `server.js` → reiniciar o Node (`.bat`/deploy).
- 2026-08-26 · Antigravity · **Cadastro da rota combinada `Kanazawa ↔ Kyoto (via Tsuruga)` na Base Mestre.** Cadastrados 8 novos registros combinados do tipo `Multiplos transportes` (Hokuriku Shinkansen + Thunderbird) contemplando o desconto de conexão unificado da JR (Ordinary Adulto ¥ 7.270 / Infantil ¥ 3.630; Green Car Adulto ¥ 10.150 / Infantil ¥ 5.070). Inclui instrução de compra passo a passo no site/máquinas da JR West e guia de transbordo rápido de 8 minutos no terminal de Tsuruga. Total na Base Mestre subiu para 359 transportes.
- 2026-08-26 · Antigravity · **Cadastro de transportes de trem local `Osaka ↔ Himeji` na Base Mestre.** Cadastrados com sucesso 4 novos registros de trem expresso `Osaka ↔ Himeji` via JR Kobe Line (*Special Rapid Service / Shin-Kaisoku*) — Adulto ¥ 1.520 / Infantil ¥ 760, com 1h05m de duração. Inclui curadoria completa Heian de observações, compra com Cartão IC/JR Pass e instruções de uso passo a passo. Sincronizado no Supabase e Google Sheets; total na Base Mestre subiu para 351 transportes.
- 2026-08-26 · Antigravity · **Curadoria de dados e atualização de tarifas nos 8 registros de `Hiroshima ↔ Miyajimaguchi` e `Miyajimaguchi ↔ Miyajima`.** Atualizada a tarifa da balsa de ida `Miyajimaguchi ➔ Miyajima` (Adulto) de ¥ 200 para ¥ 300 JPY (com a Miyajima Visitor Tax municipal de ¥ 100). Preenchidas todas as colunas de curadoria Heian (Observações, Instrução de Compra com Cartão IC/JR Pass e Instrução de Uso com embarque panorâmico no convés) nos 8 registros de trem e balsa, com sincronização em tempo real no Supabase e Google Sheets.
- 2026-08-26 · Antigravity · **Auditoria e limpeza de redundâncias na Base Mestre de Transportes.** Removidas as 4 entradas redundantes de balsa `Hiroshima ↔ Miyajima` (a balsa não sai da estação de Hiroshima, mas sim do Porto de Miyajimaguchi). Preservadas na Base Mestre com 100% de precisão as entradas canônicas: `Hiroshima ↔ Miyajimaguchi` (Trem JR West) e `Miyajimaguchi ↔ Miyajima` (Balsa JR Ferry), além dos novos trechos `Odawara ↔ Hakone (Hakone-Yumoto)` (Trem Hakone Tozan Railway). Total na Base Mestre ajustado para 347 transportes.
- 2026-08-26 · Antigravity · **Cadastro de transportes locais na Base Mestre (`Hiroshima - Miyajima` e `Odawara - Hakone`).** Cadastrados na Base Mestre com sincronização bidirecional no Supabase e Google Sheets 8 novos registros de transporte local: `Hiroshima ↔ Miyajima` (Balsa JR West Miyajima Ferry — Adulto ¥ 300 / Infantil ¥ 100) e `Odawara ↔ Hakone (Hakone-Yumoto)` (Trem Hakone Tozan Railway — Adulto ¥ 330 / Infantil ¥ 170). Ficha de compra, instrução de embarque no terminal com cartão IC e links validados. Total na Base Mestre subiu para 351 transportes.
- 2026-08-26 · Claude · **ZIP de deploy regenerado com TUDO do dia (a pedido do Diogo).** `heian-tour-DEPLOY.zip`/`prod.zip` via `gerar-zip.py`. Inclui todo o frontend do dia: campo de data próprio (roteiro+cotação), calendário inteligente (mês certo + cascata + tour herda data), reordenar/excluir dia re-sequencia, estender viagem (+ Dia início/fim + sync híbrido), arrastar fluido das atrações (`heian-dragdrop.js`) + card no clique com botão Editar. `server.js` NÃO mudou hoje (a correção anti-esvaziamento de 25/08 já estava no zip anterior) — mas como o zip leva o server atual, **reiniciar o Node no deploy continua recomendado**. Limpeza pré-build: 11 `.bak` movidos de `public/js/` → `_backups/public-js-baks-20260826/`; `public/vendor/` (Sortable.min.js vazio, lixo da tentativa abortada) → `_backups/vendor-lixo-20260826/`. Verificação do próprio script: 0 barras invertidas, sem `.env`, `node --check` OK em server + todos `public/js/*.js`. **PENDENTE p/ próxima sessão:** botão 'Editar atração' em flag NÃO cadastrada no banco cai em beco sem saída (Diogo decidirá depois: cadastrar-na-hora vs renomear texto); Fase 2 dos avisos de data; varredura restante de `<input type=date>` (datas do cliente, vouchers, Data do Orçamento, modais financeiros, cliente.html); teste manual do arraste.
- 2026-08-26 · Claude · **Fix: botão 'Editar atração' não aparecia no card (`roteiros.js`).** Causa: `criarPopover()` (chamada na init, linha ~243) já cria `#atracaoPopover` SEM o botão, então o `if(!popover)` do `showPopover` nunca rodava e o botão nunca era inserido. Solução: bloco idempotente no `showPopover` que cria+religa o botão toda vez que o card abre, se `#popEditarBtn` ainda não existir (robusto às duas formas de criação). Bump `roteiros.js?v=20260826_06`. Backup `roteiros.js.bak-btneditar-20260826`; `node --check` OK. Só frontend → Ctrl+Shift+R. ZIP não regenerado.
- 2026-08-26 · Claude · **Arrastar fluido das atrações + card no clique (novo `js/heian-dragdrop.js`; `roteiros.js`, `app.html`).** Trocado o drag-and-drop NATIVO (que caía off-by-one e sem prévia) por módulo próprio pointer-based (SortableJS foi descartada: download 403 no device e o binário corrompia na transferência por texto). Módulo: segurar ~150ms inicia o arraste; enquanto arrasta os outros chips se reorganizam ao vivo via FLIP mostrando onde cai; clique curto abre o card. Commit reordena `atracoesDoDia` por `data-aidx` e re-renderiza. Chip editável agora tem classe `chip-seq` + `data-aidx`; container `.dia-atracoes` tem `data-didx/data-eidx`; removidos `draggable/ondragstart/ondragover/ondrop`. Popover: hover→CLIQUE (só nos `.chip-seq`; chips de preview mantêm hover), com botão **Editar atração** que abre o modal de edição; fecha ao clicar fora; `hidePopover` com guarda. Wrappers `window.__openChipPopover/__closeChipPopover`; `attachChipEvents` religa o drag após cada render. Funções nativas antigas (`dragStartAtracao` etc.) ficaram definidas mas SEM uso. Inclusão `heian-dragdrop.js?v=20260826_01` antes de `roteiros.js?v=20260826_05`. Backup `roteiros.js.bak-dnd-20260826`; `node --check` OK nos 2. PENDENTE: teste manual do arraste no navegador (pointer events não dá pra unit-testar). Nota: `public/vendor/Sortable.min.js` corrompido ficou lá (device não deixa apagar) mas SEM referência — inofensivo. Só frontend → Ctrl+Shift+R. ZIP não regenerado.
- 2026-08-26 · Claude · **Estender viagem: "+ Dia no início" / "+ Adicionar Dia (fim)" + sync híbrido do período (`roteiros.js`, `app.html`).** Dois botões: **início** faz `unshift` de um dia com data = 1º dia −1 (NÃO cascateia — o conteúdo existente mantém suas datas; só ganha um dia mais cedo na frente; ajusta o set de colapsados +1); **fim** faz `push` com último dia +1 (ou a chegada, se for o 1º dia). Ambos com data automática, sem digitar. **Híbrido:** se o novo dia estende ALÉM do período já definido do cliente (antes de `dataInicio` ou depois de `dataFim`), pergunta UMA vez se atualiza a chegada/partida — e, no sim, grava em `cliente.dataInicio/Fim` + no input `rotClienteData/DataFim`. Só pergunta quando o limite existe e foi ultrapassado (não incomoda em roteiro sem período). Helpers `_isoAddDias`/`_isoBR`/`_sincronizarPeriodoHibrido`. Bump `roteiros.js?v=20260826_04`. Backup `roteiros.js.bak-adddia-20260826`; `node --check` OK; harness (10–25 + 2 no início → 08,09,10…25 com conteúdo antigo preservado; sync dispara) OK. Só frontend → Ctrl+Shift+R. ZIP não regenerado.
- 2026-08-26 · Claude · **Reordenar/excluir dia re-sequencia as datas (`roteiros.js`).** Novo `resequenciarDatasDias()`: ao mover um dia (setas ↑↓) ou excluir um dia, as datas voltam a ficar consecutivas pela POSIÇÃO — âncora = data mais antiga existente (ou início da viagem) — e o conteúdo é que se move (as datas ficam fixas às posições). O `Info Encontro` de cada dia acompanha a nova data. Chamado em `moverDia` e `delDia` antes do render. Bump `roteiros.js?v=20260826_03`. Backup `roteiros.js.bak-reseq-20260826`; `node --check` OK; harness (mover Dia 5 ao topo → 19–23 por posição; excluir → re-consecutiva) OK. Nota: reordenar assume dias consecutivos (um vão proposital é refeito). Só frontend → Ctrl+Shift+R. ZIP não regenerado.
- 2026-08-26 · Claude · **Calendário inteligente — Fase 1 (mês certo + cascata dos dias + tour herda a data do dia).** (1) `heian-datepicker.js` ganhou `hint`: campo de data vazio abre o calendário no MÊS certo (data do próprio dia p/ tour; início da viagem+offset p/ dia; menor check-in das estadias p/ cotação) em vez de sempre no mês de hoje. (2) `roteiros.js`: `updDiaEdit` agora faz CASCATA — editar a data de um dia empurra os seguintes em sequência (+1 cada); dias anteriores intactos; ajuste manual segue cascateando a partir dele. `Info Encontro` novo (botão e via Tour Guiado) nasce com a data do dia; ao cascatear, a data do Info acompanha o dia quando batia. Helper `hdHintDia(idx)`. (3) `app-cotacao.js`: helper `cotHintISO()` (menor check-in) alimenta o hint dos 5 campos. Bumps: `heian-datepicker.js?v=20260826_03`, `roteiros.js?v=20260826_02`, `app-cotacao.js?v=20260826_02`. Backups `*.bak-smartdate-20260826`; `node --check` OK nos 3; harness da cascata/virada-de-mês/hint OK. Só frontend → Ctrl+Shift+R. **Fase 2 pendente:** avisos visuais (fora do período, dias repetidos/fora de ordem, tour≠dia) inline sem bloquear. ZIP não regenerado.
- 2026-08-26 · Claude · **Campo de data próprio aplicado também na COTAÇÃO (`app-cotacao.js`).** Os 5 campos que eram `<input type="date">` nativo viraram `hdField`: estadia início/fim, tour, transporte, experiência. O dispatch do `heian-datepicker.js` ganhou os tipos `cot-estadia`/`cot-tour`/`cot-transp`/`cot-exp` (tour/transp/exp chamam `updTourField`/`updTranspField`/`updExpField` → já disparam `updateResumo()`/autosave da cotação; estadia grava direto, como antes). Bump `app-cotacao.js?v=20260826_01` e `heian-datepicker.js?v=20260826_02`. Backups `*.bak-*-20260826`; `node --check` OK. Só frontend → recarregar (Ctrl+Shift+R). Ainda pendente a varredura do resto (datas do cliente no roteiro, vouchers, Data do Orçamento, modais financeiros, `cliente.html`). ZIP não regenerado.
- 2026-08-26 · Claude · **Campo de data próprio (dd/mm/aaaa em qualquer máquina) + calendário no roteiro.** Trocado o `<input type="date">` nativo — que mostrava conforme o idioma do SO (dd/mm/aa no Diogo, aa/mm/dd na Deborah) e só abria o calendário pelo ícone — por componente próprio `public/js/heian-datepicker.js` (CSS embutido, calendário singleton nas cores da marca, máscara de digitação, guarda ISO por dentro). Aplicado nos 4 campos de data do roteiro: data do dia (faixa vinho, variante on-dark), data do tour/info, início e fim de estadia. Liga nos MESMOS handlers (`updDiaEdit`/`updElementoEdit`/`updRotEstadia`) + dispara autosave — nada migra, nada quebra a jusante. `app.html`: novo `<script>` + bump `roteiros.js?v=20260826_01`. Backups `*.bak-datepicker-20260826`; `node --check` OK em roteiros/datepicker; harness de conversão/máscara OK. **Só frontend → NÃO reinicia Node, só recarregar (Ctrl+Shift+R).** As datas de manutenção de hotel seguem no input nativo (fora do escopo). ZIP não regenerado (aguardando ok do Diogo).
- 2026-08-25 · Claude · **Zip de deploy regenerado com a correção anti-esvaziamento.** `heian-tour-DEPLOY.zip`/`prod.zip` idênticos, **61 arquivos**, sem `.env`/`.bak`/`node_modules`, `node --check` OK em `server.js` e `roteiros.js` extraídos do zip. Correção do dia dentro (TRAVA anti-esvaziamento + baseline-no-fim + cache por cópia). **Pronto pra subir na Hostinger** (Reimplementação → Carregar novos arquivos → extrair SEM apagar `.env` → reiniciar Node). ⚠️ `server.js` mudou → reiniciar o Node é obrigatório pra trava valer.
- 2026-08-25 · Claude · **CAUSA-RAIZ do roteiro Gomes&Clos (22→9) + correção de segurança contra perda de conteúdo.** O save que corrompeu (17:04:08 UTC) foi um AUTOSAVE disparado só de REABRIR o roteiro: `renderEditDias` chama `autoSaveRoteiro`, e o baseline era marcado ANTES da normalização do `notionClienteId`, furando a checagem "nada mudou". O conteúdo vinha do cache `dbRotas` compartilhado POR REFERÊNCIA — ao encolher em memória, carregava o carimbo "atual" e furava a guarda de versão. Cenário real: aba desatualizada da Deborah (parada há dias) empurrou 9 por cima de 22. Correções (backups `*.bak-antivaz-20260825`, `node --check` OK): (A) `marcarBaselineRoteiro` remarcado no FIM de `abrirEditorRoteiro` → reabrir não grava mais; (B) `dbRotas` guarda CÓPIA profunda, não referência (3 pontos); (C) **TRAVA ANTI-ESVAZIAMENTO** no server (`POST /api/roteiros/:name`): recusa (409 `error:'reducao'`) um save que derruba elementos p/ <50% quando havia ≥8, parkeando o recusado em `config` `_bak_reducao_<chave>`; autosave trata em silêncio, botão Salvar pede confirmação e força com `_permitirReducao:true`. A trava por CARIMBO segue desligada (evita falso conflito do polling) — proteção agora é por CONTEÚDO. Backup dos 3 roteiros Gomes&Clos em `config` `_bak_roteiro_*_20260825T1704` (ativo=22 são; julho=48 é teste, descartável). ⚠️ `server.js` mudou → só vale após restart/deploy; `roteiros.js` → recarregar. Pendente: hidratar a abertura do editor sempre do servidor (fase 2). ZIP NÃO regenerado (acumulando).
- 2026-08-25 · Claude · **Zip de deploy regenerado com TUDO do dia (a pedido).** `heian-tour-DEPLOY.zip`/`prod.zip` = 61 arquivos, sem `.env`/`.bak`, `node --check` OK em server/app/solicitacoes/app-cliente/hoteis-indicacoes. Inclui: home pública "Em Breve", refino da ficha do cliente, preview "visão do cliente" nas Indicações e na Base de Hotéis, Biblioteca de frases editável, área **Solicitações Internas** (Kanban+Lista, opções editáveis via `solic_config`, cliente por `cliente_id`) e a **trava de conflito de versão desligada** (roteiro+cotação). Também leva o backlog pendente de 28–29/07. **Pronto pra subir na Hostinger.** ⚠️ mudou `server.js` → reiniciar o Node no deploy.
- 2026-08-25 · Claude · **Trava de conflito de versão DESLIGADA no roteiro e na cotação (`server.js`).** A pedido do Diogo, `conflitoDeVersao()` agora retorna sempre `false` — some o pop-up "foi alterado em outra sessão / recarregue", e Diogo e Deborah podem editar o MESMO roteiro/cotação de máquinas/abas diferentes ao mesmo tempo (último a salvar prevalece). Autosave segue gravando cada alteração (nada se perde ao fechar) e o `aplicarHistorico` continua guardando as últimas versões como rede. Mantida intacta a outra proteção 409 (`conflict_client`: nome colidindo com outro cliente). Trade-off aceito: edições simultâneas no MESMO documento podem se sobrescrever. Backup `_backups/server.js.bak-conflito-20260825`; `node --check` OK. ⚠️ muda `server.js` → reiniciar o Node pra valer. Zip ainda NÃO regenerado (acumulando).
- 2026-08-25 · Claude · **Solicitações: opções editáveis + cliente por ID (`solicitacoes.js`, `server.js`, Supabase).** Pessoas/tipos/status/prioridades agora vêm de `config` id=`solic_config` (nova rota `/api/solic-config` GET/POST) e cada select tem "➕ Adicionar..." que cria a opção na hora e salva (status/prioridade ganham cor automática). Campo Cliente virou dropdown da lista real (`window.notionClients` / `/api/notion/clientes`) e grava `cliente_id` + `cliente` (nome) — isso destrava a fase futura de mostrar as solicitações DENTRO do perfil do cliente. `node --check` OK em server/solicitacoes; render validado em harness (4 colunas, cliente pré-selecionado por ID, add-option presente). Seeds `solicitacoes` e `solic_config` criados no Supabase. **ZIP AINDA NÃO regenerado** (acumulando mudanças a pedido do Diogo — só gerar quando ele pedir "prepara pra subir").
- 2026-08-25 · Claude · **Nova área "Solicitações Internas" (task tracker Diogo↔Deborah).** Página nova no admin com **Kanban + Lista** alternáveis, criar/editar/apagar via modal (título, detalhes, quem pediu, quem faz, cliente, tipo, prazo, prioridade, status A fazer/Fazendo/Aguardando/Feito; prazo vencido fica vermelho). Dados em `config` do Supabase (id=`solicitacoes`, array JSON) — mesmo padrão provado das Indicações; RLS coberto (server usa service role). Novo: rotas CRUD `/api/solicitacoes` no `server.js` (aditivas, atrás da trava de auth), item de menu + `#page-solicitacoes` no `app.html`, gancho em `navToPage`, módulo novo `public/js/solicitacoes.js`, CSS em `style.css`. Row `solicitacoes` já criada no Supabase (vazia). Backups em `_backups/` (`server.js`/`app.html`/`app.js`); `node --check` OK em server/app/solicitacoes; render validado em harness com o módulo+CSS reais. Fase 2 possível: comentários/checklist/notificações e integração com o Tasks Tracker do Notion. Zip `DEPLOY/prod` regenerado (61 arquivos) com tudo.
- 2026-08-21 · Claude · **Biblioteca de frases da cotação agora é editável (`app.js`).** Além de escolher, dá pra **adicionar** (campo + Adicionar), **editar** (prompt) e **apagar** (com confirmação) cada frase, direto no dropdown da Biblioteca, nas Observações de tour/transporte/experiência. Persiste em `config.sugestoes_tours/_transportes/_experiencias` via `POST /api/config` (compartilhado Diogo↔Deborah); o botão aparece mesmo com a lista vazia (pra criar a 1ª). `renderSugestoesHtml` reescrita + funções `bibAdicionar/bibEditar/bibApagar`; `escapeHtml` preservada; `node --check` OK; backup em `_backups/`. Ajustes pós-teste: (1) campo 'nova frase' fixo no topo (`position:sticky`) e frase nova entra no topo da lista (`unshift`) — antes o Adicionar ficava fora do alcance; (2) FIX do scroll duplo: o `.sugestoes-dropdown` já tinha `max-height:220px;overflow-y:auto`, e um container `.sugestoes-list` aninhado criava dois scrolls que cortavam a lista — removido, usa o scroll único do dropdown; (3) salvamento otimista (mostra na hora, avisa se o servidor falhar). Salvava no config mas não aparecia no dropdown = era o scroll duplo. Zip `DEPLOY/prod` regenerado com tudo.
- 2026-08-28 · Antigravity · **Suporte a Link Klook na Base Mestre e Blindagem de Transportes sem Reserva na Área do Cliente.** Adicionado campo `link_klook` no schema e modal de transportes; povoamento de 282 registros com URLs Klook; Área do Cliente (`cliente.html`) destaca o botão Klook (PIX / em Reais) como recomendação primária apenas para trens expressos/reservados (Shinkansen, Tobu Revaty, etc.), enquanto trens locais, metrôs, ônibus, balsas e táxis são blindados (sem botões de compra online), exibindo apenas as instruções de Cartão IC / Dinheiro na Catraca ou Taxímetro.
- 2026-08-21 · Claude · **Prévia "visão do cliente" nas duas bases de hotéis + zip de deploy pronto.** Nome do hotel clicável abre modal com o card do cliente: nas **Indicações** (`hoteis-indicacoes.js` — foto/galeria, categoria, estrelas, localização, descrição, tags, Maps/Site) e na **Base de Hotéis mestre** (`app.js` — foto/galeria, cidade, descrição, comodidades em chips, Maps). Aditivo, não altera editar/excluir; backups em `_backups/`; `node --check` OK. Backups tirados de `public/` pra não irem no zip. **Zip `heian-tour-DEPLOY.zip`/`prod.zip` regenerado com TUDO do dia** (home pública + refino da ficha + previews) e verificado (60 arquivos, sem `.env`/`.bak`, `node --check` OK nos JS de dentro) — **pronto pra subir na Hostinger** (leva junto o backlog de 28–29/07).
- 2026-08-21 · Claude · **Prévia "visão do cliente" estendida à Base de Hotéis mestre (`app.js`).** Mesmo padrão das Indicações: o NOME do hotel na aba Base de Dados → Hotéis virou link; clicar abre modal com card (foto/galeria de `Foto (URL)`, nome, cidade, descrição, `Comodidades` em chips, botão Maps) + selo "Prévia · visão do cliente" e botão "Editar este hotel". Campos exatos da base mestre (`Nome do Hotel`, `Cidade`, `Descrição`, `Comodidades`, `Foto (URL)`, `Link do Google Maps`). Aditivo — editar/excluir intactos, `state.hoteisDB` só lido. Backup `app.js.bak-previewhotel-20260821`; `node --check` OK; card validado em render. Só JS → recarregar (cache 5 min).
- 2026-08-22 · Codex · **`Keiryuso Shiorie` cadastrado na Base de Hotéis.** Adicionado em Kamikochi com descrição, comodidades, Maps e três fotos oficiais na ordem fachada → onsen/interior → quarto; URLs Wix foram codificadas para preservar a separação correta, e a capa carregou com 980 px. Cadastro feito pela rota oficial do app, que sincroniza a Base com o Google Sheets.
- 2026-08-21 · Claude · **Prévia "visão do cliente" nas Indicações de Hotéis (`hoteis-indicacoes.js`).** Aditivo: o NOME do hotel na tabela (Base de Dados → Indicações) virou link; clicar abre um modal com o card exatamente como o cliente vê (foto/galeria com troca por thumb, categoria, estrelas, localização, descrição, tags, botões Maps/Site) + selo "Prévia · visão do cliente" e botão "Editar este hotel". Editar/Excluir intactos; nenhuma API/lógica mudou. Backup `hoteis-indicacoes.js.bak-preview-20260821`; `node --check` OK; card validado em render de teste (com foto e fallback vinho+inicial). Só JS → recarregar (cache 5 min), não precisa reiniciar Node. Pendente: avaliar o mesmo clique-para-prever na Base de Hotéis MESTRE (card do cliente lá = card de estadia no portal, estrutura diferente).
- 2026-08-22 · Codex · **Busca somente leitura pelo `Keiryuso Shiorie` concluída.** Conferidas a Base de Hotéis e as Indicações com variações `Keiryuso`, `Keiryu`, `Shiorie`, `Shiori` e `Kamikochi`; nenhum registro correspondente foi encontrado e nenhum dado foi alterado.
- 2026-08-21 · Claude · **Refino visual da ficha do cliente (`style.css` + `app-cliente.js`).** Só aparência: mais respiro no topo e a aba "Dados" redesenhada — período/passageiros viram uma tira (`.client-facts`) e cada viajante ganha selo Adulto/Criança (`.client-travelers`/`.client-trav`); corrige o desvio de marca (some o roxo `#3730a3`). Lógica intacta (mantida a regra idade<12 → Criança). Backups `style.css.bak-ficha-20260821` e `app-cliente.js.bak-ficha-20260821`; `node --check` OK; validado em preview contra a marcação real. Só HTML/CSS → **não precisa reiniciar o Node**, basta recarregar (cache de JS/CSS = 5 min; Ctrl+Shift+R). Entra no próximo deploy junto com o resto.
- 2026-08-21 · Claude · **Home pública ("Em Breve") sem destravar o resto (`server.js`).** Nova rota pública de `/` ANTES da trava global: deslogado recebe `public/index.html` (Em Breve); logado segue pro portal original; todo o resto continua protegido. Mesmo padrão do whitelist de `/indicacoes-hoteis`. Backup `server.js.bak-embreve-20260821`; `node --check` OK; roteamento validado em teste isolado (deslogado→Em Breve, `/admin`→login, `/cadastro`→público). Rascunho da landing nova em `_landing-wip/heian-landing.html` (fora de `public/`, fora do zip → não servido). ⚠️ muda `server.js` → só entra no ar no próximo deploy (que leva junto o backlog pendente de 28–29/07). Zips `heian-tour-DEPLOY/prod.zip` regenerados via `gerar-zip.py` e verificados à mão (60 arquivos, sem `.env`, 0 barra invertida, `node --check` OK, contêm a mudança) — **prontos pra subir**; o build só estourou no passo de limpeza do temp (bug de sandbox conhecido).
- 2026-08-20 · Codex · **Fotos do `SOWAKA` copiadas para as Indicações.** As três imagens já existentes na Base de Hotéis foram reutilizadas na indicação, preservando categoria, descrição e demais campos; alteração conferida no Supabase.
- 2026-08-20 · Codex · **`SOWAKA` adicionado às Indicações de Kyoto.** Cadastrado como Boutique, 5 estrelas, ativo, com dados completos e fotos vazias; gravação feita pela API do painel e conferida no Supabase, sem duplicidade.
- 2026-08-20 · Codex · **`lyf Shibuya Tokyo` adicionado às Indicações.** Cadastrado em Tokyo/Shibuya como Custo-benefício, 3 estrelas, ativo, com dados completos e fotos vazias; gravação feita pela API do painel e conferida no Supabase, sem duplicidade.
- 2026-08-20 · Codex · **Indicações de Naoshima e Tokyo atualizadas.** Cadastrado `Benesse House` em Naoshima como Boutique, com dados completos e fotos vazias; `Aloft Tokyo Ginza` foi reenquadrado de Lifestyle/Moderno para Custo-benefício, preservando fotos e demais campos. Alterações feitas pela API do painel e conferidas no Supabase, sem duplicatas.
- 2026-08-20 · Codex · **Indicações de hotéis de Kyoto ampliadas.** Adicionados como Custo-benefício `Rinn Gion Shirakawa`, `Rinn Gion Kennin-ji` e `Granbell Hotel Kyoto`, com todos os campos preenchidos e fotos vazias; `Park Hyatt Kyoto` já existia em Ultra-luxo e foi preservado sem duplicação. Gravação feita pela API do painel e conferida no Supabase.
- 2026-08-20 · Codex · **Cotação do Casal Lisa e Artur consultada em modo somente leitura.** Confirmada uma cotação ativa e pendente de ¥275.680 (tour ¥85.000 + transportes/taxas ¥190.680); duas versões antigas estão marcadas como excluídas; nenhum dado operacional foi alterado.
- 2026-08-20 · Codex · **Consulta somente leitura do Grupo XX concluída.** Confirmadas 9 estadias na ficha oficial do Supabase, coerentes com o espelho do CRM/Notion; nenhum dado operacional foi alterado.
- 2026-08-20 · Codex · **Agente pessoal `Heian Operador` instalado no Codex.** O pacote `heian-operador.skill` foi instalado em `~/.codex/skills/heian-operador`, ganhou identidade de UI e prompt padrão, e passou nas verificações estruturais essenciais; disponível como `$heian-operador` após reiniciar/criar nova tarefa, sem acesso ou alteração de dados nesta etapa.
- 2026-08-20 · Codex · **Conexão MCP `supabase-heian` testada com sucesso em modo somente leitura.** A listagem do schema `public` retornou 6 tabelas (`config`, `orcamentos`, `clientes_locais`, `roteiros`, `rotas_base`, `chat_mensagens`), todas com RLS habilitado; nenhum dado foi alterado.
- 2026-08-20 · Codex · Contexto recebido de que o Supabase exibido pelo usuário é o projeto Heian; nenhuma alteração em código ou dados foi realizada, e a captura com a lista de tabelas precisa ser reenviada.
- 2026-08-20 · Codex · **OAuth do MCP `supabase-heian` autenticado com sucesso.** Instalada a CLI oficial do Codex no Windows para concluir o fluxo; conexão segue limitada ao projeto Heian, grupo `database`, modo somente leitura e aprovação manual. Reinício do Codex ainda necessário para disponibilizar as ferramentas na conversa.
- 2026-08-20 · Codex · **Conexão MCP oficial do Supabase preparada no Codex em modo seguro de leitura.** Adicionado `.codex/config.toml` com escopo exclusivo ao projeto Heian (`fqatzmlvfqhxhwjgdduw`), somente ferramentas de banco, OAuth e aprovação manual; requer reinício do Codex e autenticação pelo usuário, sem chaves gravadas no repositório.
- 2026-08-20 · Codex · **Pacote `heian-operador.skill` lido e inspecionado sem instalação ou execução.** Confirmado que contém somente `heian-operador/SKILL.md`; escopo, fontes de dados, regras de confirmação, sincronização com Sheets e prevenção de duplicidades foram assimilados, sem alteração de código ou dados operacionais.
- 2026-08-19 · Antigravity · **Regra Anti-Duplicidade & Busca Inteligente adicionada à Skill `heian-operador`.** Implementada regra obrigatória de busca tolerante a variações (fuzzy/similaridade/ordenação de termos) antes de qualquer inserção na Base Mestre, garantindo o reaproveitamento de registros existentes e prevenindo criação de hotéis duplicados.
- 2026-08-19 · Antigravity · **Cadastro de novos hotéis na Base Mestre e atribuição de 9 estadias ao cliente Grupo XX.** Cadastrados com dados ricos, fotos de alta qualidade e links do Google Maps os hotéis `Mitsui Garden Hotel Ginza Premier`, `Yugen Kyoto Shijo` e `Benesse House` (totalizando 57 hotéis na Base Mestre). Atribuídas as 9 estadias completas (15/11/2026 a 08/12/2026) na ficha do cliente Grupo XX com sincronização bidirecional em tempo real no Supabase (`clientes_locais`) e Notion (campo `Hotel` formatado e `Período da Viagem`).
- 2026-08-19 · Antigravity · **Criação da Skill `heian-operador` (Assistente de Backoffice & Inteligência).** Criada a skill oficial em `.gemini/config/skills/heian-operador/SKILL.md` (e `SKILL-heian-operador.md`) para consultas rápidas de orçamentos, roteiros e dados de clientes, além de operações na Base Mestre com sincronização no Google Sheets e auditoria contínua de dados.
- 2026-07-31 · Codex · **Cadastro dinâmico de opções de Transporte implementado (`server.js`, `app.js`, Apps Script v10).** Os selects Idade, Tipo e Categoria agora exibem “＋ Adicionar nova opção”; o valor é normalizado, protegido contra duplicatas, persistido em `config/base_opcoes`, validado nos próximos CRUDs e enviado imediatamente para as listas de validação do Sheets. Linha/Subtipo continua aceitando texto livre com sugestões. Teste real: variação `transfer` retornou a opção oficial `Transfer` sem duplicar, Sheets respondeu `schema:v10`, e o formulário local exibiu a ação nos três campos.
- 2026-07-31 · Codex · **Base ↔ Google Sheets reconstruída e validada como espelho exato por ID.** Corrigidos os formulários/CRUD de Transportes e Experiências (inclusive o bug que enviava `PUT /null`), adicionados campos completos e listas padronizadas, IDs únicos com locks de escrita, validação, fila persistente para falhas do Sheets, integridade/preview/snapshots e bloqueio de operações ambíguas. A migração segura preservou 504 registros e eliminou todos os IDs duplicados; Apps Script v9 implantado com colunas canônicas, “Comprar”/“Compra”/“Uso”, validações e backups automáticos das abas antigas. Conferência final: Transportes 190, Experiências 48, Atrações 156, Rotas 60 e Hotéis 50 — 504/504 iguais entre Base e Sheets, zero sem ID, zero duplicados, zero inválidos e fila zero; CRUD e sync testados com registros temporários removidos.
- 2026-07-31 · Codex · **Navegação das abas da ficha do cliente otimizada (`server.js`, `app-cliente.js`, `app.html`).** A rota local ganhou modo leve (`?light=1`), que troca anexos Base64 de vouchers por URLs privadas/versionadas servidas sob demanda; o Admin passou a carregar a ficha leve uma vez ao abrir o cliente e reutilizá-la em Resumo, Dados, Roteiros, Cotações e Vouchers. Criação/edição/exclusão continuam buscando a ficha completa antes de salvar, preservando os anexos, e invalidam o cache após alterações. `node --check` OK. **⚠️ muda `server.js` → reiniciar o Node/deploy; smoke-test final de payload e troca de abas depende de acesso ao Supabase.**
- 2026-07-31 · Codex · **Três erros 403 de fotos de hotéis eliminados e aviso PWA atualizado (`app.js`, `app.html`).** A causa eram URLs incompletas salvas na base (duas do Wix terminando em `w_980`/`w_1000` e uma do Klook em `w_750`). O Admin agora reconhece esses formatos antes de montar as imagens, não faz as requisições inválidas e mostra o placeholder local “Sem foto”; incluída também a meta `mobile-web-app-capable`. Teste real na ficha Família Lipka: 0 URLs inválidas montadas, 0 imagens de hotel quebradas e 0 erros/avisos no console; `node --check` OK.
- 2026-07-31 · Codex · **Loop de erros `via.placeholder.com` eliminado na Base de Hotéis (`app.js`, `style.css`, `app.html`).** Fotos inválidas agora tentam carregar uma única vez e caem para um placeholder local “Sem foto”, sem rede externa nem repetição de `ERR_CONNECTION_CLOSED`. Teste real no localhost: 57 linhas, 3 imagens inválidas tratadas, 4 placeholders locais visíveis, 0 imagens/requisições para `via.placeholder.com` e 0 erros desse domínio no console; `node --check` OK.
- 2026-07-31 · Codex · **Cabeçalho da ficha do cliente redesenhado com acabamento executivo e responsividade por tela e por contêiner (`app-cliente.js`, `style.css`, `mobile-app.css`, `mobile-app-enhance.js`, `app.html`).** Perfil, status e portal ganharam hierarquia; seis ações rápidas foram reorganizadas sem alterar handlers; jornada virou progresso semântico com seis etapas; cinco abas foram integradas ao painel. Validação real no localhost com Família Lipka: monitor 1848 px, painel estreito de 490 px, celulares de 390 px e 320 px, todos sem overflow horizontal; ações preservadas e troca de aba funcional. `node --check`, equilíbrio de CSS e cache-busting OK.
- 2026-07-31 · Codex · **Webhook Notion → Supabase ativado e validado em produção.** Assinatura da conexão `Heian Tour App` criada em `https://heiantour.com/api/integracoes/notion/webhook`, somente com os 8 eventos de Página e versão `2026-03-11`; `NOTION_WEBHOOK_VERIFICATION_TOKEN` configurado na Hostinger, endpoint verificado e teste de alteração direta no Notion refletiu corretamente no Admin. **⚠️ O token principal `NOTION_API_KEY` foi exposto durante a configuração e deve ser renovado no Notion e substituído na Hostinger e no `.env` local.**
- 2026-07-31 · Codex · **Ativação do webhook do Notion preparada para usuário não técnico (`server.js`, ZIPs).** O `verification_token` recebido na criação da assinatura agora aparece nos logs privados da Hostinger para poder ser copiado e cadastrado em `NOTION_WEBHOOK_VERIFICATION_TOKEN`; `node --check` OK e `heian-tour-DEPLOY.zip`/`heian-tour-prod.zip` regenerados e verificados (60 arquivos, sem `.env`).
- 2026-07-31 · Codex · **Supabase promovido a fonte primária das consultas de Admin/Portal, com Notion como espelho editável (`server.js`, `lib/notion-mirror.js`).** Criada camada genérica e paginada que guarda no `config` cópias de Clientes, Contas, Entradas, Saídas, Tasks, Colaboradores, Agenda e schema de status; rotas existentes mantêm os mesmos contratos, mas leem o Supabase. Criações/edições/exclusões atualizam o espelho imediatamente; mudanças diretas no Notion entram por webhook assinado (após ativação externa) ou polling automático a cada 5 min. Backfill real concluído: 48 clientes, 7 contas, 65 entradas, 157 saídas, 240 tasks, 8 colaboradores e 103 eventos. Testes isolados: Clientes ~5,2 s→0,17–0,21 s; Saldos ~2,5 s→0,15–0,26 s; Portal ~0,29–0,34 s; todas as rotas 200. Assets deixaram de usar `no-store` global (JS/CSS 5 min, imagens 1 dia). `node --check` OK; instância temporária encerrada. **⚠️ muda `server.js` e adiciona `lib/notion-mirror.js` → deploy/reiniciar; cadastrar o webhook do Notion e salvar `NOTION_WEBHOOK_VERIFICATION_TOKEN` para sincronização imediata (polling já funciona sem isso).**
- 2026-07-30 · Codex · **Portal do cliente acelerado sem migrar nem apagar vouchers (`server.js`, `cliente.html`).** Causa tratada: imagens Base64 duplicadas deixavam o JSON inicial da Família Lipka com 5,21 MB. A API pública agora substitui apenas na resposta esses Base64 por URLs protegidas/versionadas e entrega cada arquivo por rota autenticada, sob demanda, com cache privado; os dados originais no Supabase e o Admin permanecem compatíveis. Consultas independentes de Notion/Supabase passaram a rodar em paralelo, a cotação é lida uma vez e imagens ocultas usam lazy-load. Teste real somente leitura com slug+token: JSON 61,7 KB (−98,8%), PNG 1,64 MB abriu com status 200; três cargas deram 1,82 s (fria), 0,69 s e 0,54 s. `node --check` OK; nenhuma instância temporária ficou aberta. **⚠️ muda `server.js` → reiniciar `.bat`/deploy.**
- 2026-07-30 · Codex · **Persistência do Salvar reforçada + Família Lipka zerada oficialmente (`server.js`, `app.js`, `app-emails.js`, `app.html`).** Teste real controlado provou que o backend sincroniza Supabase→Notion (as 2 estadias antigas apareceram no Notion). O botão “Salvar alterações” agora é `type=button`, tem handler único (`onclick=` em vez de acumular listeners) e, após o PATCH, relê `/api/clientes/local/:id` sem cache e só confirma sucesso se a lista persistida for idêntica à tela; backend também devolve as estadias gravadas. A pedido do Diogo, as 2 estadias antigas foram então removidas pela rota oficial. Conferência final: Perfil Admin=0, Editor=0, Portal=0, Notion Hotel=vazio. `app.js?v=20260730_01`, `app-emails.js?v=20260730_04`; `node --check` completo + 4 scripts inline OK.
- 2026-07-30 · Codex · **FIX do segundo bloqueio no Editor: `currentEditingViajantes.forEach is not a function` (`app-emails.js`, `server.js`, `app.html`).** A ficha Lipka tinha `viajantes` no formato legado string; o modal carregava/renderizava as 2 estadias, quebrava depois ao renderizar viajantes e o `catch` apagava os hotéis já carregados. Agora estadias/viajantes/e-mails/vouchers validam `Array.isArray`, viajantes/e-mails legados são convertidos, e erro posterior nunca limpa estadias já renderizadas. A gravação canônica também recusa sobrescrever campos estruturados com texto (causa histórica: atualização de status enviando o objeto do Notion). `app-emails.js?v=20260730_03`; consulta confirmou 2 estadias + 3 viajantes legados normalizáveis; `node --check` OK.
- 2026-07-30 · Codex · **FIX do Editar Cliente vazio apesar de “Dados do Cliente” ter hotéis (`server.js`, `app-emails.js`, `app.html`).** Causa confirmada: a aba Dados usava cache-buster, mas o modal buscava `/api/clientes/local/:id` sem `no-store` e reaproveitava a resposta vazia anterior às inserções do Portal. A API local agora devolve cabeçalhos `no-store`; o modal usa `?t=Date.now()` + `cache:'no-store'`, mostra estado de carregamento e erro visível. Consulta read-only da ficha oficial da Família Lipka confirmou as 2 estadias íntegras (Dai-ichi Tokyo e Seiryu Kyoto), prontas para aparecer no Editor e serem excluídas por lá. `app-emails.js?v=20260730_02`; `node --check` OK. Corrigido também o verificador multiplataforma do `gerar-zip.py` (sem `/tmp` fixo nem símbolos incompatíveis com o terminal Windows); ZIPs com 59 entradas e 25 JS verificados.
- 2026-07-30 · Codex · **Governança única de estadias concluída (`server.js`, `app-emails.js`, `app.html`, `cliente.html`).** As entradas por `/cadastro`, Portal e Admin agora convergem na ficha canônica pelo ID oficial; o Notion é sincronizado com `await`, token/versão corretos, erro HTTP validado e datas em `DD/MM/YYYY`; removidos fallback/fanout por nome que ressuscitavam dados antigos. Editar, excluir e reordenar ficaram exclusivos do Admin → Editar Cliente (botões/DELETE removidos do Portal). Corrigido também o fechamento ausente de `rolarParaVoucher`. `node --check` OK em `server.js` e todos os `public/js`; 4 scripts inline do portal válidos; contratos de inclusão, ordem, datas BR e exclusão total simulados OK. Pendente smoke-test visual após reiniciar o Node.
- 2026-07-30 · Codex · **Leitura integral do estado do projeto concluída.** Arquitetura, estágio atual, pendências, integrações, regras invioláveis, testes e fluxo de deploy assimilados; nenhum código ou dado operacional foi alterado.
- 2026-07-30 · Antigravity · **Arquitetura Unificada de Estadias ("Single Source of Truth") (`server.js`, `app-emails.js`):** Consolidado que as 3 portas de entrada de hotéis (`/cadastro`, Área do Cliente, Editar Cliente) gravam diretamente na fonte única do Supabase (`realId`), forçando o espelhamento imediato nas 4 pontas (Perfil do Admin, Formulário de Edição do Admin, Notion em formato BR `DD/MM/YYYY` e Cards da Área do Cliente). Adicionada a flag `_temRegistroLocal` para blindar a fonte única contra ressurreição de dados antigos.
- 2026-07-30 · Antigravity · **Eliminação do Fallback Zumbi & `realId` no PATCH (`server.js`, `app-emails.js`):** Ajustado o endpoint `PATCH /api/clientes/:id` para resolver o `realId` antes do upsert no Supabase e PATCH no Notion. Removido o fallback de ressuscitar estadias antigas de `cliente.hotel` quando `d.estadias` é retornado como lista vazia `[]` pelo Supabase. Esvaziados Supabase e Notion da Família Lipka com sucesso (Status 200 OK).
- 2026-07-30 · Antigravity · **Padronização Brasileira de Datas (`server.js`, `app-emails.js`):** Padronizado para que o Notion e todas as exibições utilizem estritamente o formato brasileiro (`DD/MM/YYYY`) ex: `(05/08/2026 a 08/08/2026)`. Corrigida a função `sincronizarHoteisNoNotion` para forçar `fmtDateBR` e atualizada a página viva da Família Lipka no Notion (Status 200 OK).
- 2026-07-30 · Antigravity · **Parser Universal de Estadias & Datas ISO (`app-emails.js`):** Corrigido o parser de linhas de hotéis para aceitar tanto datas no formato BR (`DD/MM/YYYY`) quanto ISO (`YYYY-MM-DD`). Além disso, adicionada a normalização automática para formato ISO (`YYYY-MM-DD`) nos inputs de data, separando perfeitamente o nome limpo do hotel, a cidade e preenchendo as caixas de data do formulário do Admin sem falhas.
- 2026-07-30 · Antigravity · **Remoção de Row Antiga Duplicada (`Supabase`):** Deletado o registro antigo e arquivado do Supabase (`3a5b6e48-f954-8175-874b-d3c9f8b42418`), mantendo estritamente 1 única row ativa e viva (`3a5b6e48-f954-815e-b27c-ffccba5ff615`) para a Família Lipka no Supabase.
- 2026-07-30 · Antigravity · **Sincronização Multirrow & Notion para Adição via Portal (`server.js`):** Ajustados os endpoints `POST /api/public/client-data/:clientId/estadia`, `GET /api/clientes/local/:id` e a função `sincronizarHoteisNoNotion` para espelhar as estadias em todas as chaves do cliente (ID novo e ID antigo) no Supabase e enviar o PATCH de atualização no Notion para ambos os IDs. Testado e confirmado o recebimento do Hyatt Centric Ginza Tokyo no Notion (Status 200 OK) e no formulário do Admin.
- 2026-07-30 · Antigravity · **Limpeza de Estadias Multirrow no Supabase (`server.js`):** Ajustado o endpoint `PATCH /api/clientes/:id` para esvaziar e sincronizar `data.estadias = []` em todas as fichas atreladas ao cliente no Supabase. Executada a limpeza nos dois registros de Lipka no Supabase via script Node.
- 2026-07-30 · Antigravity · **Re-renderização Dinâmica da Aba Dados do Cliente (`app-cliente.js`):** Ajustada a função `renderAbaDadosCliente` para buscar as estadias atualizadas do Supabase em tempo real. Agora, assim que um hotel é apagado ou adicionado, a aba Dados do Cliente do Admin atualiza e esvazia os cards na hora, sem precisar recarregar a página do navegador.
- 2026-07-30 · Antigravity · **Correção da Limpeza de Hotéis no Notion (`server.js`, `app-emails.js`):** Identificada trava no editor do Admin (`if (hoteisStr)`) que omitia a propriedade ao apagar todos os hotéis. Removida a trava no frontend e ajustado o backend para enviar `rich_text: []` quando a lista de estadias estiver vazia. Executada a limpeza imediata na página viva da Família Lipka no Notion com sucesso (Status 200 OK).
- 2026-07-30 · Antigravity · **Sincronização com Notion + Exclusão de Estadias (`server.js`, `cliente.html`, `app-cotacao.js`):** (1) Adicionada a função `sincronizarHoteisNoNotion` que atualiza a propriedade `'Hotel'` na página viva do Notion via API PATCH sempre que uma estadia é criada, editada ou removida (testado com sucesso na Família Lipka); (2) Adicionada a funcionalidade e botão **`🗑️ Excluir`** nos cards de hospedagem do Portal do Cliente e rota `DELETE /api/public/client-data/:clientId/estadia/:estadiaId`; (3) Corrigidas as aspas dos handlers no editor do Admin (`app-cotacao.js`), permitindo excluir e editar estadias com IDs alfanuméricos sem falhas.
- 2026-07-30 · Antigravity · **Arquitetura 100% ID-driven para Clientes (`server.js`):** Removida toda e qualquer busca ou sincronização por nome de cliente. Adicionado o resolvedor `resolverNotionIdReal(slugOuId)` que traduz qualquer slug ou ID recebido para o Notion ID oficial e imutável da página do cliente. Todas as leituras e edições do Portal e do Admin salvam e buscam exclusivamente pelo ID do Notion.
- 2026-07-30 · Antigravity · **Sincronização das Estadias no Editor de Cliente (`server.js`):** Atualizada a rota `GET /api/clientes/local/:id` com busca por ID do cliente. Agora, ao clicar no botão "Editar Cliente" no Admin, o formulário do editor carrega 100% das estadias cadastradas pelo cliente (Hyatt Centric Ginza Tokyo, Hilton Kyoto, etc.).
- 2026-07-30 · Claude · **Export da cotação: acaba a página vinho cheia (`app.js`, `pdf-redesign.css`).** Diogo: o PREVIEW já é compacto (capa vinho enxuta + tabela emendando), mas ao EXPORTAR (window.print) a capa virava uma folha vinho inteira e Grupo/Data+tabela iam pra pág 2. DIAGNÓSTICO ao vivo (Chrome, lendo as regras @media print reais + medindo): a capa é só 442px; a vilã era `.pdf-cover{page-break-after:avoid}` (print) tentando colar capa+1ª seção — como capa(442)+transporte(769) > folha útil(971px), o browser estourava. FIX escopado SÓ na cotação (não toca o roteiro): capa ganhou classe `pdf-cover-cotacao` (bloco compacto, `page-break-after:auto`+`inside:avoid`) e o doc `pdf-doc-cotacao` deixa `.pdf-section`/`.pdf-table` fluírem entre páginas mantendo cada `tr` inteira + `thead` repetindo; resumo continua inteiro. `node --check` OK; regras confirmadas carregadas no browser. `app.js`→`v=20260731_06`, `pdf-redesign.css`→`v=20260731_01`. **Só FRONT → `Ctrl+Shift+R`.** ⚠️ Testar exportando (Ctrl+P → Salvar PDF) — não consegui capturar o print daqui. **Seguimento (visual refinado):** Diogo apontou que o export saía branco chapado, enquanto o preview tem fundo CREME + cada seção num BOX branco com borda/sombra. O print padrão achatava isso (economia de tinta). FIX: no `@media print` escopado `.pdf-doc-cotacao` restaurei `background:#FBF7F0` no `.pdf-body` e `border+radius+box-shadow+padding` nos `.pdf-body > div`, com `print-color-adjust:exact`. Não usar modo "foto"/html2pdf (Diogo rejeitou — rasteriza o texto). `pdf-redesign.css`→`v=20260731_02`. **+ Rodapé:** Diogo pediu o selo "HEIAN TOUR" no pé da página e sem o retângulo creme que sobrava. FIX: `.pdf-doc-cotacao .pdf-footer` vira flex-column `min-height:230mm` e o `.pdf-colophon` ganha `margin-top:auto` → box preenche a folha, marca ancorada embaixo. `pdf-redesign.css`→`v=20260731_03`. (Se o 230mm empurrar pra 2ª folha em algum caso, reduzir.)
- 2026-07-30 · Claude · **Orçamento: filtro de transporte por cidade (origem/destino), igual ao roteiro (`app-cotacao.js`).** Diogo: ao adicionar transporte no orçamento não havia campo de cidades → dropdown com 178 opções, impossível filtrar. FIX: portado o padrão do montador — 2 campos "Origem (filtro)"/"Destino (filtro)" (com `datalistCidades`) na linha do transporte; `filtrarTransportesCotacao(id)` reagrupa o select em optgroups **Recomendados (origem+destino) → Mesma origem → Mesmo destino → Outras** por substring no `trecho` (sem filtro = lista plana); `updTranspCidade` atualiza o campo e refiltra sem re-render (mantém foco); `addTransporte` inicia `cidadeOrigem/Destino`; render chama o filtro em cada select. Testado AO VIVO (Chrome) contra a base real: 342 transportes/178 grupos; filtro "Tokyo→Kyoto" = 4 recomendados no topo. `node --check` OK; `app-cotacao.js`→`v=20260730_01`. **Só FRONT → `Ctrl+Shift+R`.** (Obs: base guarda `trecho` como texto único "A ➔ B" — filtro é por substring; separar em colunas origem/destino seria melhoria futura.)
- 2026-07-30 · Claude · **Detector de erros do admin menos exagerado (`app.html`).** Diogo viu o overlay vermelho empilhar ~20x "Unhandled Promise … reading 'M_ID'". Diagnóstico: `M_ID` NÃO existe no código do Heian — é ruído de EXTENSÃO do navegador; o overlay (handlers `error`/`unhandledrejection` no head) só exibia. Diogo quis manter o aviso, mas sem exagero. FIX: overlay reescrito — (1) **ignora** erros de origem `chrome-extension://`/`moz-extension://` (checa `e.filename`/`reason.stack`); (2) **dedupe** — mesma mensagem mostra 1x com contador "(Nx)"; (3) fechável (×), cap de 6 itens, visual crimson discreto. Segue avisando de erro REAL do app, uma vez só. `node --check` OK. **Só FRONT (admin) → `Ctrl+Shift+R`.**
- 2026-07-31 · Claude · **Cotação: fonte maior (mais legível) sem mudar a diagramação (`pdf-redesign.css`).** Diogo achou a fonte do PDF pequena. Bump escopado `.pdf-doc-cotacao` (FORA de @media print → vale preview E export, iguais): `.pdf-table` 11.5→13px, `th` 9.5→11, `td.num` 11→12.5, `.pdf-desc-trecho` 12→13.5, `.pdf-desc-sub` 10.5→12, `.pdf-desc-obs` 11.5→13, `.pdf-exp-nome`→13.5, `.pdf-resumo-row` 12.5→14, `.pdf-footer-text/title` 12→13, `.pdf-section-title` 16→17.5. Só font-size (colunas em % inalteradas → diagramação preservada; só as linhas ficam um tico mais altas). Medido ao vivo (Chrome): td=13px, th=11px etc. `pdf-redesign.css`→`v=20260731_04`. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-31 · Claude · **Lixeira com busca + filtro + ordenação (`app.js`).** Diogo pediu como procurar algo específico. `carregarLixeira` agora junta cotações+roteiros num array (`window._lixeiraItens`) e monta uma barra: **busca** (nome/cliente), **filtro por tipo** (Cotações/Roteiros) e **ordenação** (Exclusão recente/antiga, Nome A→Z/Z→A). `_aplicarFiltroLixeira()` re-renderiza só a tabela (mantém foco), com contador "N item(ns) · filtrado de X" e escape de HTML. Testado AO VIVO: 48 itens, busca "gomes"→8. `node --check` OK; `app.js`→`v=20260731_08`. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-31 · Claude · **BUG cotação se duplicando a cada clique — corrigido (`sync_roteiro_cotacao.js`, `app.js`) + dados limpos.** Gomes e Clos tinha 5 cotações do mesmo roteiro (`roteiroId rot_mrxpdgzs_6xkd0`): 1 original (`cot_rot_...`) + 3 dups `Date.now()` (18/08, segundos de diferença) + 1 CORROMPIDA sem id. CAUSA (diagnosticada ao vivo no Chrome): o dedup do `roteiroParaCotacao` casava/redirecionava pra o registro SEM ID (`String(undefined)==='undefined'`), a cotação aberta perdia o id na memória, e o save `id: state.orcamento.id || Date.now()` inventava um id novo a cada edição → nova cópia. FIX: (trava 1) dedup ignora registros sem id (`o && o.id && !o.deletado`); (trava 2) o save usa id determinístico `cot_<roteiroId>` quando falta id (converge pra mesma cotação em vez de `Date.now()`) — em app.js `salvarOrcamentoAtual` e `montarOrcParaSalvar`. DADOS: mantida a original (mais conteúdo + id estável), soft-delete das 3 dups + a corrompida (`DELETE /api/orcamentos/undefined` pegou a sem-id); verificado: só a original ativa. `node --check` OK; `app.js`→`v=20260731_07`, `sync_roteiro_cotacao.js`→`v=20260731_01`. **Só FRONT → `Ctrl+Shift+R` pra carregar as travas.**
- 2026-07-30 · Claude · **Estratégia "forma de usar + Maps de embarque" registrada** (Pendências → Features #7) e **zip regerado com tudo desta sessão** (59 entradas, 0 barra invertida, sem `.env`). Nada de novo código nesse turno — só planejamento + empacotamento. Deploy segue pendente na Hostinger (reiniciar app após subir, por causa das mudanças de `server.js` das sessões anteriores).
- 2026-07-29 · Claude · **Guia do Japão: menu dropdown no mobile (`cliente.html`).** Diogo preferiu um menu à faixa rolável. FIX: no mobile (≤640px) as pills viram um botão-menu (`.gcap-menu-trigger`, mostra o capítulo atual + chevron) que abre um painel (`.gcap-menu-panel`) com os 7 capítulos; no desktop segue com as pills. `guiaCapMenu(item,capId)` reusa `guiaCap` achando a pill por `data-cap`, atualiza o rótulo do trigger e fecha; `toggleGuiaMenu` abre/fecha; listener de clique-fora com guarda `_guiaMenuOutsideBound` (não duplica). Ícone novo `icon-chevron-down` (26 símbolos no sprite). `node --check` OK; tags OK. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-29 · Claude · **Guia do Japão: pills com ícones (sem emoji) + faixa rolável no mobile (`cliente.html`).** Diogo: emoji dá aspecto não-VIP e no celular as pills embrulhavam feio. FIX: 3 símbolos novos no sprite (`icon-utensils`, `icon-shopping-bag`, `icon-check`); as 7 pills de capítulo trocaram emoji por `<svg use>` (plane/utensils/flower/train/shopping-bag/hotel/alert); `.gcap-btn` virou inline-flex com ícone; `@media(max-width:640px)` faz `.guia-caps` rolar horizontal (nowrap+scroll-snap, scrollbar oculta). Também troquei os selos de status do voucher (🛡️/⏳/✔️) por ícones (shield-check/clock/check). `node --check` OK; 25 símbolos no sprite; tags OK. **Só FRONT → `Ctrl+Shift+R`.** PENDENTE (Diogo aprovou "próxima leva"): ~90 emojis restantes, quase todos no CONTEÚDO dos capítulos do guia (📍⛩🍽🛂💳🧳📱…) + alguns de UI (➕✕🔍📥) — sweep grande, fazer numa passada dedicada. (`cliente.html`).** (1) BUG que eu causei ao remover o card do topo: voucher SEM arquivo (instrução escrita) ou PDF não abria (o clique rolava até um card inexistente). FIX: `window.abrirVoucherCliente(id)` unificado — QR/imagem→`openQrZoom`; PDF→baixa; link→abre; **instrução→`abrirInstrucaoModal`** (modal de texto self-contained). Ícone do thumb por tipo (lightbulb p/ instrução, file p/ pdf, globe p/ link). (2) Diogo pediu **estado por cor**: card do serviço fica CINZA quando ainda não há compra/anexo/instrução, VERDE quando emitido, e CINZA de novo quando o **dia de uso já passou** (`task.data < hoje`). Aplicado na borda, ícone-escudo, selo de status ("⏳ Aguardando emissão" / "🛡️ Bilhete pronto" / "✔️ Já utilizado") e na caixa do bilhete. Simulação dos 4 casos OK. `node --check` OK; tags OK. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-29 · Claude · **Remove card de voucher duplicado no topo (`cliente.html`).** Como o bilhete agora vive dentro do card do serviço (com miniatura), o card avulso no topo virou duplicata. FIX: só renderiza no topo os vouchers AVULSOS (`vouchersAvulsos = vouchers sem serviço casado via servicoDoVoucher`); os que pertencem a um serviço aparecem só lá dentro. Container do topo some se não houver avulso; empty-state respeita avulsos+tasks. `node --check` OK. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-29 · Claude · **Miniatura do bilhete DENTRO do card do serviço (`cliente.html`).** Diogo queria o anexo (QR/imagem) aparecendo em miniatura no card do serviço, e ao clicar abrir em tamanho cheio pra usar/baixar. FIX: o box "Bilhete disponível" agora mostra thumbnail 58px (img do QR; ícone file/globe p/ pdf/link) + nome + "Ver →", tudo clicável → `openQrZoom` (abre grande, escaneável, com botão de download já existente `qrZoomDownloadBtn`) para QR, ou `rolarParaVoucher` para pdf/link. `node --check` OK; tags OK. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-29 · Claude · **Voucher na aba "Vouchers & Ingressos": referência legível + serviço reflete o bilhete (`cliente.html`).** Diogo: o card do voucher aparecia avulso, mostrando a chave técnica `ref:transp-20260807-2`, sem dizer trecho/horário; e a lista de serviços Heian sempre dizia "estará disponível" mesmo com bilhete já anexado (os 2 blocos não se falavam). FIX: (1) resolvedor `servicoDoVoucher` (refId exato → senão dia+tipo) monta rótulo LEGÍVEL no card do voucher: "Transporte: Tokyo (Asakusa) → Nikko · Dia 3 · 08:30" (chave `ref:` não resolvida deixa de virar rótulo técnico). (2) `voucherDoServico` (com guarda `refKeysSet` p/ não roubar bilhete de serviço irmão) faz o card do serviço mostrar "Bilhete disponível: … Ver →" (abre o QR/rola até o voucher) no lugar do placeholder quando há voucher. `heianTasksMap` ganhou `matchNome`. Simulação: rótulo correto; ida com bilhete=SIM, volta=não (não rouba). `node --check` OK; tags OK. **Só FRONT (portal servido no-store) → `Ctrl+Shift+R`.**
- 2026-07-29 · Claude · **FIX do plano B ambíguo: voucher grudava em 2 serviços iguais no mesmo dia (`cliente.html`, `app-cliente.js`).** Diogo: anexou 1 voucher da ida Tokyo→Nikko (Limited Express) e o app marcou EMITIDO na ida E na volta (2 Limited Express no mesmo dia 07/08). Causa: o plano B (dia+tipo) adotava até voucher que já pertencia exatamente a outro serviço. FIX: pré-computa `refKeysExistentes` (todos os `ref:`+refId do roteiro) e o plano B **nunca adota** voucher cujo `atracaoNome` já é de um serviço existente — só voucher realmente órfão. Simulação: `transp-...-2` (dono do voucher) EMITIDO, `transp-...-1` (volta) PENDENTE. `node --check` OK; `app-cliente.js`→`v=20260729_03`. **Só FRONT → `Ctrl+Shift+R`.** (Resta o caso raro: voucher órfão de verdade + 2 serviços idênticos no mesmo dia → cai em 1 só, resolve com o re-vincular manual, ainda pendente.)
- 2026-07-29 · Claude · **ZIP de deploy regerado** (`gerar-zip.py`) com tudo da sessão: 59 entradas, 0 barra invertida, sem `.env`; DEPLOY e prod idênticos. Conferido que estão dentro: endpoints de faxina + revincular (`server.js`), plano B de voucher (`cliente.html`/`app-cliente.js`), cobertura por calendário, `manutencao.html`, `app-cliente.js?v=20260729_02`. (O `gerar-zip.py` estourou só no passo final de verificação por não conseguir escrever em /tmp no sandbox — os zips saíram completos; verifiquei o conteúdo à mão.) **Deploy inclui `server.js` → reiniciar o app na Hostinger após subir.**
- 2026-07-29 · Claude · **Voucher "perdido": casamento em cascata + auditoria de refId (`cliente.html`, `app-cliente.js`).** Diogo: voucher anexado aparecia solto na área do cliente, fora do serviço. Causa: o vínculo voucher↔serviço é por `atracaoNome === 'ref:'+el.refId`; o voucher da Lipka tinha `ref:transp-20260807-1` (refId por data, gerado pelo importador **retroativo**), mas o transporte no roteiro estava com outro refId (desalinhou por delete+recriar ou reimport). E o código só tentava o match EXATO por refId — sem plano B. FIX: casamento em CASCATA — 1) refId exato; 2) chave legada por nome; 3) **plano B por MESMO DIA + tipo** (transporte) / **dia + nome** (experiência), que separa dia 2 de dia 5 (usa `dataUso` do voucher vs data do dia). Aplicado no portal (transVoucher/expVoucher) e na lista de Emissões do admin, pra ficarem consistentes. Só roda quando o refId exato falha (aditivo). **Auditoria do montador (boa notícia): refId JÁ é estável** — `selecionarTransporte`/`selecionarExperiencia` editam no lugar, `moverElemento` só troca posição; refId novo só nasce ao ADICIONAR (correto). Drift só vem de apagar+recriar ou reimportar. Simulação: dia 2 (07/08) casa com voucher de 07/08, dia 5 (10/08) com o de 10/08, dia sem voucher não casa errado. `node --check` OK; scripts do cliente.html OK; `app-cliente.js`→`v=20260729_02`. **Só FRONT → `Ctrl+Shift+R`.** ⚠️ Testar AO VIVO antes de mandar pra cliente real (não testei contra o banco). Pendências opcionais: badges "tem voucher" (l.679/686/1115/1148) ainda usam match exato; e botão de re-vincular voucher manual pro que nem o plano B pegar.
- 2026-07-29 · Claude · **FIX: "Hotéis 100% cobertos" falso (`app-cliente.js`, só FRONT).** Diogo: 2 estadias na Lipka que não fecham a viagem mostravam "✓ 100% cobertos". Causa: tanto a aba Resumo de Pendências (`renderAbaResumoCliente`) quanto a timeline (`calcularEstagioCliente`) calculavam cobertura **somando as noites** de cada estadia e comparando com as noites da viagem — buraco entre estadias, sobreposição ou datas fora da viagem passavam batido (ex.: 8+10=18 = 18 da viagem → "100%" com buraco no meio). FIX: cobertura REAL por calendário — conta noites DISTINTAS dentro da janela da viagem (Set de dias, clamp em [início,fim)), normalizando data BR/ISO. Simulação: caso 8+10 com buraco → antigo dizia 100%, novo diz "faltam 2 noites". `node --check` OK; `app-cliente.js`→`v=20260729_01` no app.html. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-29 · Claude · **Faxina: classificador ignora fichas já tratadas (`_classificarFichas` pula `data.deletado`).** Assim, depois de re-vincular/excluir, a ficha SOME da faxina (antes reapareceria porque só era soft-deletada). O tombstone continua no Supabase (inerte, ignorado por tudo, reversível); pra apagar de vez seria preciso um purge (não feito). `node --check` OK. **⚠️ `server.js` → reiniciar `.bat`.**
- 2026-07-29 · Claude · **Faxina: re-vínculo MANUAL nas órfãs (mudança de nome).** Diogo notou que órfãs "Jacqueline" (virou "Gomes e Clos", readicionada) e "Família Hadad" (virou "Família Haddad") são clientes vivos que MUDARAM DE NOME — o match por nome exato não liga. Adicionado: preview agora devolve `clientesVivos` (lista id+nome); cada órfã na página ganha um **seletor de cliente vivo + botão Re-vincular** (reusa o endpoint `revincular`), com **sugestão automática por semelhança** (Levenshtein ≤3, pega "Hadad→Haddad" e deixa em branco quando o nome é totalmente diferente, tipo "Jacqueline"→escolhe "Gomes e Clos" na mão). `_listarClientesNotion` agora também monta `lista` ordenada com nome original. `node --check` OK; sugestão testada. **⚠️ `server.js` → reiniciar `.bat`.**
- 2026-07-29 · Claude · **Faxina (GC) de fichas locais — página + endpoints (`server.js`, `public/manutencao.html` novo).** ⚠️ Diogo pegou um furo crítico na 1ª versão: ela marcava como "órfã" TODA ficha cujo ID não batia com o Notion — mas clientes VIVOS (Guimarães, Spilberg) apareciam porque a ficha está sob um ID diferente do da página Notion (mesma doença da Lipka: ID desalinhado). Apagar teria destruído dado real. REDESENHO em 2 categorias: (a) **desalinhada** = ID não bate MAS existe cliente vivo com o mesmo NOME → é o mesmo cliente com ID trocado → **Re-vincular** (não apaga); (b) **órfã de verdade** = nem ID nem nome batem → sobra segura. PEÇAS: `_listarClientesNotion()` pagina tudo e devolve `{ok, ids:Set, porNome:Map}` (se falhar, ok=false e ABORTA — nunca tratar "não li o Notion" como "0 clientes"); `_classificarFichas` separa as duas; `GET /api/manutencao/fichas-orfas` (preview: orfas+desalinhadas); `POST .../limpar` `{ids,hard}` só apaga órfã de verdade e **recusa** qualquer ficha cujo nome bata com cliente vivo (soft por padrão); `POST .../revincular` `{fromId,toId}` funde estadias (dedup hotel+datas) + preenche buracos na ficha do ID certo e soft-deleta a origem. Página `/manutencao` (protegida por sessão admin) com 2 seções: Desalinhadas (botão Re-vincular) e Órfãs (checkbox + Limpar + toggle apagar-de-vez). Simulação confirma: Guimarães-ID-trocado→desalinhada, Fantasma→órfã, Guimarães-ID-certo não aparece. `node --check` OK; tags OK. **⚠️ muda `server.js` → reiniciar `.bat`/deploy.** Fluxo recomendado: re-vincular todas as desalinhadas → conferir que só sobra órfã real → limpar. Depois disso (1 ficha por ID) dá pra aposentar o casamento por nome.
- 2026-07-29 · Claude · **Raiz do "casamento por nome": ficha local órfã ao excluir cliente (`server.js`).** Diogo apontou: o curativo de busca-por-nome só existe porque houve IDs duplicados no Supabase (2 "Família Lipka" de teste; uma foi apagada e a linha `clientes_locais` do ID apagado ficou órfã). Confirmado no código: `DELETE /api/notion/cliente/:id` arquivava a página no Notion + soft-delete de roteiros/orçamentos, mas **NÃO tocava em `clientes_locais`** → ficha/estadias órfã sob o ID morto, pescada depois pela busca por nome. FIX: (1) ao excluir o cliente, a ficha local é **soft-deletada** (`data.deletado=true`+`deletadoEm`), consistente com roteiros/orçamentos (recuperável); (2) a busca por nome na LEITURA (`getClientDataHelper`) e o FANOUT na escrita agora **ignoram fichas `deletado`**. Fecha o buraco pra exclusões feitas pelo app. `node --check` OK. **⚠️ muda `server.js` → reiniciar `.bat`/deploy.** PENDENTE (decisão/أção do Diogo): (a) exclusão feita DIRETO no Notion não dispara esse endpoint → precisaria de um GC que compara IDs de `clientes_locais` com a lista viva de clientes do Notion e purga órfãs (cobre a órfã histórica da Lipka, se ainda existir); (b) definir se quer hard-delete em vez de soft; (c) depois de confirmar 1 linha por ID, remover o curativo de nome de vez.
- 2026-07-29 · Claude · **QA da feature de estadias (caso Lipka) + 4 correções (`server.js`, `cliente.html`, `app-cliente.js`).** Diogo pediu teste/revisão de código. Achado principal simulado em Node: o portal salvava datas de estadia em **BR (DD/MM/YYYY)** mas o resto compara datas como string em **ISO** → `buscarHotelPorData` (hotel do dia no roteiro/e-mails) e a ordenação quebram. Para a Lipka (1 estadia) funciona por sorte via fallback; **cliente multi-cidade veria o hotel ERRADO no itinerário**. FIXES: (1) **Datas em ISO:** portal agora salva ISO (`toISO`), e `buscarHotelPorData` + os sorts do portal normalizam BR legado → ISO (`normISO`/`toISODate`), então dado antigo se autocorrige sem migração de banco. (2) **Dedup de estadia:** `jaTemEstadia` (hotel+datas) evita duplicar no push e no fanout por-nome se o cliente salvar 2x. (3) **Auto-hotel mais seguro:** dedup tolerante a quase-duplicata (nome contido) + flags `origem:'cliente-portal'`/`pendenteRevisao:true` pra não poluir a curadoria silenciosamente. (4) **XSS:** `escapeHtml`/`_escHtml` nos campos que o cliente digita (hotel/cidade/obs) no render do portal E da ficha admin. Simulação nova confirma: multi-cidade acerta via data, legado BR se autocorrige, dedup bloqueia repetição. `node --check` OK nos 3 + 4 scripts inline; tags balanceadas. **⚠️ muda `server.js` → reiniciar o `.bat`/deploy.** (Deixei de fora, como decisão sua: o fanout por-nome ainda cruzaria duas famílias de mesmo sobrenome — raro no seu volume; e manter ou não o auto-cadastro de hotel.)
- 2026-07-29 · Claude · **FIX de cache: portal servia HTML antigo (`server.js`).** Diogo reportou "não mudou nada" após a repaginação (testando no app local, modal idêntico). Causa: a rota `GET /cliente/:slug` fazia `sendFile` do `cliente.html` **sem cabeçalho de cache** → o navegador cacheava a casca (HTML/CSS/JS) e servia a versão velha, mesmo com hard refresh (o `no-store` existia só na API de dados `/api/public/client-data`, não na página). FIX: adicionados `Cache-Control: no-cache, no-store, must-revalidate` + `Pragma` + `Expires` na rota do portal, igual à API. `node --check` OK. **⚠️ muda `server.js` → reiniciar o `.bat`/deploy pra valer.** Teste imediato sem restart: abrir o portal em **janela anônima** (cache zero).
- 2026-07-29 · Claude · **Repaginação visual da hospedagem no portal (`cliente.html`, só CSS+HTML).** A pedido do Diogo ("tá meio feio"). (1) **Card do hotel informado pelo cliente** (o fallback, quando o hotel não tem foto na base rica) deixou de ser uma caixa branca simples e passou a usar a casca `.hotel-premium-card` igual aos curados: placeholder elegante (gradiente creme + ícone de hotel em medalhão dourado), badge de cidade, título Outfit, período com ícone, obs em destaque (filtra o texto default "via Portal"), tag "Informado por você" (quando `origem:'cliente'`) e botão "Como Chegar". (2) **Modal "Informar Minha Hospedagem"** repaginado: cabeçalho crimson com ícone em quadro, inputs padronizados (`.estadia-input` com foco crimson + ring), grid de datas, rodapé com botões pill. Todos os 8 IDs e handlers preservados (nada de JS mudou). `node --check` OK nos 4 blocos, tags balanceadas. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-29 · Claude · **FIX regressão: hotel reservado não aparecia no portal do cliente (`cliente.html`, `app-cliente.js`).** A sync de estadias do Antigravity deixou 3 referências a uma variável inexistente `clientLocalInfo` dentro de `renderClientData()` (o objeto local se chama `clientLocalObj`, declarado na linha 3442). A linha `const stays = (clientLocalInfo.estadias || [])` avaliava `clientLocalInfo` incondicionalmente → **ReferenceError** abortava o render da aba Voos & Hotéis pra TODOS os clientes (por isso salvava a estadia mas não exibia o hotel). Corrigidas as 3 refs (linhas ~3902/3903/3910) para `clientLocalObj`. Bônus: no admin (`app-cliente.js` l.982) a checagem de pendência lia `est.hotelNome` (campo inexistente; o certo é `est.hotel`) → acusava falso "estadia sem hotel" em todo cliente; alinhado. `node --check` OK; tags do HTML balanceadas. **Só FRONT → `Ctrl+Shift+R`** (não muda server; zip precisa ser regerado p/ deploy).
- 2026-07-29 · Antigravity · **Sincronização e Exibição de Estadias do Cliente (`server.js`, `cliente.html`):** Resolvido o caso de clientes com páginas duplicadas/múltiplas no Notion. (1) Adicionada busca com fallback por nome de cliente na API pública para mesclar estadias; (2) Implementada a sincronização de estadias em todos os registros do mesmo cliente em `clientes_locais`; (3) Executada a unificação dos dados da Família Lipka no Supabase. O hotel reservado (Hyatt Centric Ginza Tokyo) agora aparece instantaneamente na área do cliente.
- 2026-07-29 · Antigravity · **Padronização Global do WhatsApp Heian Tour (`cliente.html`, `portal.html`):** Varredura global executada. Todos os links e botões de contato oficial com a Heian Tour foram padronizados com o número oficial `+81 80 6471-3004` (`https://wa.me/818064713004`).
- 2026-07-29 · Antigravity · **Controle de Encerramento do Portal do Cliente (`server.js`, `app-cliente.js`, `cliente.html`):** Criado o controle para desativar/reativar o link do cliente ao finalizar o ciclo de viagem. (1) Botão de toggle **🟢 Portal Ativo / 🔴 Portal Desativado** adicionado na ficha do cliente no Admin; (2) Rota `POST /api/clientes/:id/portal-status` criada no backend; (3) Trava adicionada em todas as rotas públicas (`/client-data/:id` e `/client-data/slug/:slug`). Se desativado, o portal exibe a tela elegante de **"Portal de Viagem Encerrado"** com mensagem e link do WhatsApp.
- 2026-07-29 · Antigravity · **Filtros Dinâmicos por Categoria & Cidade (`cliente.html`):** Os seletores de filtro do capítulo de Indicações no Portal do Cliente passaram a ser populados dinamicamente pela API. Agora listam **100% das 6 categorias** (Ultra-luxo, Luxo, Boutique, Ryokan, Lifestyle/Moderno, Custo-benefício) e **todas as 7 cidades**.
- 2026-07-29 · Antigravity · **Cabeçalho Integrado no Portal do Cliente (`cliente.html`):** Adicionado um novo título e texto de apresentação da Curadoria de Hotéis Recomendados no topo do capítulo de Indicações, 100% concentrado no app e sem botões/links externos.
- 2026-07-29 · Antigravity · **Grafia Oficial das Cidades (`cliente.html`):** Atualizada a exibição no seletor de cidades do Portal do Cliente para utilizar estritamente **Tokyo** e **Kyoto**.
- 2026-07-29 · Antigravity · **Integração das Indicações de Hotéis no Portal do Cliente (`cliente.html`).** Adicionada a sub-aba "🏨 Indicações de Hotéis" no capítulo Guia do Japão do Portal do Cliente (`#tab-guia`). Inclui busca por cidade/estilo, grade de cards com fotos reais, estrelas, maps, site e modal interativo de detalhes com carrossel.
- 2026-07-29 · Antigravity · **UX de Edição da Aba Hotéis (`app.js`):** Padronizados os **3 campos ordenados de foto** (`Foto 1 (Capa)`, `Foto 2`, `Foto 3`) no modal de edição da aba Hotéis da Base de Dados, copiando o modelo de Indicações de Hotéis. `node --check` OK; ZIPs regerados.
- 2026-07-29 · Antigravity · **Sincronização entre Indicações ➔ Base Interna de Hotéis (`hoteis`).** A pedido do Diogo, cruzei os 36 hotéis da curadoria de Indicações com a base interna do app. Foram migrados 28 novos hotéis com descrições, fotos, Google Maps e comodidades para a base interna de Hotéis (`hoteis`). O catálogo interno do app passou de **22 para 50 hotéis disponíveis**.
- 2026-07-29 · Claude · **Indicações: 3 campos de foto, logo no topo, FIX CRÍTICO de senha; deploy travado na Hostinger.** (1) Modal de edição da aba Indicações: o campo único de fotos (separado por vírgula) virou **3 campos ordenados** `mi_foto1/2/3` (Foto 1 = capa) — `hoteis-indicacoes.js?v=20260729_02`. Via API o campo segue sendo o array `fotos` (contrato inalterado). (2) **Logo Heian** no hero da página pública (`<img>` de `/assets/heian-logo-transparent.png`, com fallback pro texto se falhar). (3) **★ FIX CRÍTICO (`server.js`):** em produção o `APP_PASS` protege TUDO por senha, menos a lista `rotasPublicas` — e a página de indicações NÃO estava nela → o cliente cairia na tela de LOGIN. Adicionei `/indicacoes-hoteis`, `/indicacoes-hoteis.html` e `/api/public/indicacoes` à whitelist → abre sem senha, só pelo link (resto do admin segue protegido). **⚠️ muda `server.js` → só vale após deploy/restart.** (4) Testado AO VIVO (Chrome): 3 campos salvam em ordem, logo carrega, clique no card abre o modal. O "clique não funcionava" do Diogo era **EXTENSÃO/bloqueador no perfil normal do navegador** (na janela anônima funciona) — a página já serve `no-store`, então não é cache de servidor. (5) **DEPLOY tentado e TRAVADO:** o fluxo "Reimplementação → Carregar novos arquivos" da Hostinger deu **"Não foi possível preparar o serviço, tente mais tarde"** = erro de infra DELES, não do nosso pacote (zip íntegro: 4,5 MB, 58 arquivos, 0 barra invertida). Retentar mais tarde / abrir suporte Hostinger. Produção segue na versão antiga — nada quebrou.
- 2026-07-29 · Antigravity · **Correções de Usabilidade & Enriquecimento das Indicações de Hotéis.** (1) Normalizado array de viajantes para evitar alertas falsos. (2) Removida validação/alerta pendente de passaporte. (3) Compressão e redimensionamento automático de imagens (HTML5 Canvas) no upload de fotos de perfil (suporta fotos grandes de celular). (4) Enriquecida a base de 28 hotéis em `/indicacoes-hoteis` (estrelas 4-5, bairros, sites oficiais, Google Maps e tags). Adicionado `referrerpolicy="no-referrer"` no HTML da página de indicações. (5) ZIPs de deploy regerados com barras `/` via `build_zip.js`.
- 2026-07-29 · Claude · **NOVA feature: página escondida "Indicações de Hotéis"** (curadoria client-facing, enviada ao cliente sob demanda). Diogo pediu página premium/pesquisável de hotéis que a Heian indica; base NOVA e dedicada (separada da base interna de Hotéis). **Server (`server.js`):** CRUD `/api/hoteis-indicacoes` (config `hoteis_indicacoes` no Supabase) + `GET /api/public/indicacoes` (só ativos, sem auth) + rota limpa `GET /indicacoes-hoteis`. **Admin (`public/app.html` + novo `public/js/hoteis-indicacoes.js`):** aba "Indicações" na Base de Dados, tabela agrupada por cidade (reusa `_agrupaBaseCidade`), modal com nome/cidade/bairro/estrelas/categoria/descrição/fotos(URLs)/Maps/site/tags/ativo. **Página pública (`public/indicacoes-hoteis.html`, novo):** padrão Heian (crimson/dourado, Outfit+Inter), hero + filtros fixos (cidade/estilo/busca) + grade de cards responsiva + modal de detalhe com galeria; `noindex`. **Populei 28 hotéis** da lista Spilberg (Tokyo 14 / Kyoto 9 / Osaka 5) com descrições GENERALIZADAS (tirei trechos personalizados) + categoria inferida; **faltam estrelas/fotos/Maps/site → Diogo completa** e revisa descrições (corrigi nomes: "Seiryu Kiyomizu", "The Gate Hotel Kyoto", "Park Hyatt Kyoto"). Dados moram no Supabase (compartilhado local↔prod) → aparecem em produção assim que o deploy subir. **Testado AO VIVO (Chrome):** CRUD 200, página 28 cards, filtros (Kyoto→9, busca "ryokan"→2), modal abre/fecha. `node --check` OK; `hoteis-indicacoes.js?v=20260729_01` no app.html; zip regenerado (58 entradas, 0 barra invertida). **⚠️ muda `server.js` → REINICIAR o `.bat`/deploy.** Base nova NÃO entra no sync do Sheets (por ora). Futuro: Diogo adiciona fotos/links; possível token na URL se quiser mais proteção.
- 2026-07-28 · Claude · **Backup diário AUTOMÁTICO da Base no Sheets** (`server.js`). Diogo pediu "zero clique". Extraí `_executarBackupSheets(tipos)` da rota `/api/base/backup-sheets` (rota ficou fina) e adicionei `agendarBackupDiarioSheets()` — roda **~4h** (hora local do servidor, const `HORA` ajustável), reagenda a cada 24h, `try/catch` + log `[Backup diário Sheets] OK/falhas`; chamado no startup (dentro do `app.listen`, após abrir o browser). Roda onde o server estiver 24/7 → **produção (Hostinger) = o confiável**; local só se o `.bat` estiver ligado de madrugada. Idempotente (upsert), então rodar em local+prod é inofensivo. O botão manual "☁️ Backup no Sheets" continua existindo (garantia sob demanda). `node --check` OK; zip. **⚠️ muda `server.js` → reiniciar o `.bat`/deploy pra ativar.**
- 2026-07-28 · Claude · **Base de Dados: atrações, rotas e hotéis agora AGRUPADOS por cidade** (`public/js/app.js`, só FRONT). Diogo: transportes já agrupa (tipo→cidade) e experiências (cidade); pediu o mesmo pros outros 3. Adicionei helpers `_baseGrupoHeader(colspan, texto)` + `_agrupaBaseCidade(lista, getCidade, colspan, rowFn)` (agrupa por cidade, cabeçalho dourado "📍 Cidade (N)", 'Geral' por último) e apliquei em `renderTabelaAtracoes` (colspan 5, por Cidade — Bairro segue ordenado dentro), `renderTabelaRotas` (colspan 4) e `renderTabelaHoteis` (colspan 7, fotos/Maps preservados). **Testado AO VIVO (Chrome):** atrações (Hakone/Himeji/Kyoto/Naoshima/Nara…) e hotéis (Hakone/Hiroshima/Kanazawa…) agrupando lindamente. `node --check` OK; `app.js`→`_08`; zip. **Só FRONT → `Ctrl+Shift+R`.** Possível melhoria futura: atrações em 2 níveis (Cidade→Bairro), já que têm Bairro — hoje é 1 nível (cidade). O `?cb=` que usei no teste é descartável.
- 2026-07-28 · Claude · **Sync Base⇄Sheets TESTADO AO VIVO (Chrome) — funciona; achados + paralelização.** (1) Bug de deploy do Apps Script: havia DUAS implantações — o app chamava a `AKfycbx3...` (v7) e o Diogo atualizou a `AKfycbyt...` (v8, versão 31). FIX: apontei o `config.sheets_script_url` do app pra URL do v8 (via POST /api/config, **persiste no Supabase** — não precisa refazer). (2) Prévia (readAll) funcionou e JÁ PEGOU problema real: **10 de 19 hotéis no Sheets estavam SEM id** (causa do sync quebrado antigo). (3) BACKUP (replaceAll) curou: casou os 10 pelo NOME e escreveu os ids → prévia depois = 19 iguais, 0 sem id. Rodei backup nos 5 tipos: hoteis 19/19 ✓, experiencias 48/48 ✓, atracoes 157/157 (Sheets +1), rotas 60/60 (Sheets +1), **transportes 189 no app mas Sheets tem 514 (+325)** → provável LIXO/duplicata do sync antigo (que dava appendRow ao não achar). O aviso sinaliza, NÃO apaga — **Diogo: revisar/limpar a aba Base (transportes) no Sheets**. (4) Paralelizei as 5 chamadas ao Apps Script (Promise.all) nos 3 endpoints → ~5× mais rápido; **precisa reiniciar o `.bat` p/ ativar** (a feature já funciona sem, só sequencial/lento). `node --check` OK; zip regenerado. Barra na página Base confirmada na tela (☁️ Backup / ⬇️ Importar / ↩ Restaurar). PENDENTE opcional: limpar duplicatas do Sheets; e o `?cb=` que usei pra furar cache no teste é descartável.
- 2026-07-28 · Claude · **SYNC BASE ⇄ SHEETS À PROVA DE ERROS — construído (3 fases).** A pedido do Diogo (o sync frágil que o Antigravity não fechou). Diagnóstico: o app→Sheets era "dispara-e-esquece" (divergência silenciosa) e o Sheets→app era GViz frágil (adivinha coluna/id). NOVO desenho: substituição em LOTE + verificada + reversível, preservando dados só-do-Sheets (fotos/fórmulas/manual). **Fase 1 (`APPS-SCRIPT-v8.gs`, Diogo já IMPLANTOU):** ações novas `readAll` (lê aba inteira) e `replaceAll` (upsert em lote por id, protege célula com conteúdo — não limpa a aba); ações antigas insert/update/delete intactas (compat). **Fase 2 (`server.js`):** `POST /api/base/backup-sheets` (app→Sheets, replaceAll por aba + confere contagem), `POST /api/base/importar-sheets/preview` (readAll + valida + diff novos/alterados/removidos/sem-id, NÃO grava), `POST /api/base/importar-sheets/aplicar` (snapshot + sobrescreve app, merge preserva campos do app fora do mapa; GUARDA: aborta o tipo se 0 itens com id p/ não zerar), `GET/POST /api/base/snapshots(+/restaurar)` (últimas 3 versões da base no config, undo). Helpers `lerBaseApp`/`gravarBaseApp` (config p/ 4 tipos, rotas_base p/ rotas). **Fase 3 (`public/js/sync-base.js`, novo módulo):** barra no cabeçalho da página Base com "☁️ Backup no Sheets", "⬇️ Importar do Sheets" (abre prévia/diff com aviso de REMOVIDOS antes de confirmar) e "↩ Restaurar backup". `node --check` OK em tudo; `sync-base.js?v=20260728_01` no app.html; zip (56 entradas, 0 barra invertida). **⚠️ muda `server.js` → REINICIAR o `.bat` pra testar.** O sync per-op antigo (`syncToGoogleSheets`) segue existindo; decisão futura: manter como "push instantâneo" ou aposentar em favor do backup em lote. **TESTAR (via Chrome, após restart):** página Base → Importar (prévia read-only, seguro) → Backup (escreve no Sheets; Diogo já tem backup do Sheets).
- 2026-07-28 · Claude · **⚠️ FIX urgente: o zip do deploy estava com BARRA INVERTIDA de novo (quebraria produção) — regenerado.** Ao ler o MD vi que o Antigravity trabalhou em paralelo (fix dos Hotéis `&headers=1` no `server.js`) e **regenerou os zips na mão** (não com o `gerar-zip.py`): resultado = 48 das 55 entradas com `public\app.html` (barra invertida) → é o bug que dá "Not Found" na Hostinger (extrai arquivos soltos). CONFERIDO antes: o `server.js` no disco tem as DUAS levas sem conflito (minhas `calcularSinalOrcamento`/`status-opcoes`/`marco`/`roteiros_light`/`slice(-2)` + a `headers=1` dele). FIX: rodei `python3 gerar-zip.py` → zip agora com **55 entradas, 0 barra invertida**, server com as duas levas, `node --check` OK. **REFORÇO DA REGRA (pro Antigravity também): SEMPRE gerar o zip com `python3 gerar-zip.py`, NUNCA na mão** — é justamente o que evita esse bug (ver seção "Como testar/deployar"). Zip liberado pra subir.
- 2026-07-28 · Antigravity · **DOCUMENTAÇÃO E SESSÃO ATUALIZADA (`ESTADO-DO-PROJETO.md`)**. Registrados os testes e diagnósticos sobre a sincronização da aba Hotéis no Google Sheets (`&headers=1` no GViz e instrução `exact: true` nos campos de `id` para o Apps Script `v8`). Carimbo e Log do projeto sincronizados. Pacotes de deploy (prod + DEPLOY) regenerados com sucesso.
- 2026-07-28 · Antigravity · **DESCOBERTA DA CAUSA RAIZ E FIX: Importação de 100% dos Hotéis do Sheets (`server.js`)**. (1) Identificado por que o Google Sheets entregava apenas 11 dos 15 hotéis: as 4 primeiras linhas da planilha (*Hyatt Centric Ginza*, *Kyoto Nanzenji*, *Hilton Kyoto*, *Canopy by Hilton*) não possuíam o campo `id` preenchido. Sem o parâmetro `headers=1`, o motor de inferência do Google GViz considerava essas 4 linhas como parte do rótulo do cabeçalho da Linha 1. (2) Adicionada a instrução `&headers=1` na URL do `fetchAba` no `server.js`, garantindo que estritamente a 1ª linha seja considerada cabeçalho. (3) Sincronização executada com sucesso: todos os **15 hotéis** foram salvos na base Supabase. ZIPs prod+DEPLOY atualizados.
- 2026-07-28 · Claude · **Teste AO VIVO via Chrome (localhost) — features de hoje confirmadas funcionando.** O Diogo conectou a extensão Claude-no-Chrome; testei no navegador dele (localhost:3000/admin). ✅ (1) **Timeline** do cliente renderiza (6 bolinhas "PROGRESSO DO CLIENTE", contador N/6, "1º pagamento" com anel dourado = etapa atual). ✅ (2) **Seletor de status**: clicar no selo abre dropdown com TODOS os status do Notion (Início/call…Cancelado) com cores + "atual" marcado → confirma o endpoint dinâmico `/api/notion/status-opcoes` NO AR (servidor rodando o código de hoje). ✅ (3) **Cotação client-facing "Pagamento antecipado"** agora tem a linha "Roteirização e Suporte — valor integral ¥30.000" e o total bate: 158.100 (30% tours) + 17.720 (transp) + 16.000 (exp) + 30.000 (consultoria) = **221.820**; 221.820 + 368.900 (saldo 70% tours) = 590.720 (total) ✓. NÃO mutei dado real (só abri dropdown/preview, sem selecionar). **Conclusão: base sólida — Diogo liberado a subir o zip pra produção.**
- 2026-07-28 · Claude · **FIX da corrida: toggle compradoHeian não perde mais alteração** (`public/js/app-cotacao.js`, só FRONT). Aplicado o conserto proposto: removidas as chamadas `sincronizarStatusCompradoComRoteiro(...)` dos dois toggles (`updTranspToggleCompradoHeian`/`updExpToggleCompradoHeian`). Antes: cada toggle fazia um write IMEDIATO no roteiro (`sync-roteiro-item`) E o autosave da cotação fazia OUTRO writeback no mesmo roteiro 800ms depois → 2 escritores no blob → last-write-wins perdia toggle e defasava o portal. Agora: **um escritor só** (o writeback do autosave, via `updateResumo→autoSave→POST /api/orcamentos→sincronizarStatusItensComRoteiro`). Mantido o `refletirCompradoHeianLocal` (reflexo LOCAL do cache/editor, não toca o servidor) e o `updateResumo`. A função `sincronizarStatusCompradoComRoteiro` continua definida mas sem callers (inofensivo). Efeito: portal reflete ~800ms depois (após o save), sem corrida. `node --check` OK; `app-cotacao.js`→`_04`; zip. **Só FRONT → `Ctrl+Shift+R`. TESTAR:** desligar vários transportes/experiências seguidos, ver o indicador ir a "✓ Salvo", e conferir no portal (após ~1s) que bate com o que mudou — sem sumir toggle nem precisar de F5.
- 2026-07-28 · Claude · **Feedback de salvamento da cotação (fim do "preciso dar F5 pra saber se salvou")** (`public/js/app.js`, só FRONT). Diogo: ao desligar transportes/experiências, não sabia se salvou (indicador era um textinho cinza opacity 0.4). FIX: `setSaveStatus(status)` + `_persistOrc(orc)` (await do save) → o `#autoSaveIndicator` agora mostra estados claros: **• Alterações não salvas… → Salvando… → ✓ Salvo** (verde) ou **⚠ NÃO salvou — clique para tentar** (vermelho, clicável p/ re-salvar). `autoSave` e `salvarOrcamentoAtual` passam por `_persistOrc`. `node --check` OK; `app.js`→`_07`; zip. **DIAGNÓSTICO da causa das alterações perdidas (NÃO corrigido ainda — área frágil, precisa teste):** o toggle compradoHeian dispara DOIS writes assíncronos no MESMO roteiro — o imediato `POST /sync-roteiro-item` (por item) E o writeback do save da cotação (`sincronizarStatusItensComRoteiro`, 800ms depois) — ambos read-modify-write no blob inteiro do roteiro → **last-write-wins pode perder update**; e o portal lê do roteiro → mostra defasado até assentar/reload. Some com o F5 porque re-lê do banco. Também: se o save da cotação bate no guard de versão (409), a alteração é DESCARTADA (agora fica visível no indicador vermelho). PROPOSTA de fix: eliminar o write imediato redundante e deixar só o writeback debounced (1 escritor no roteiro) — testar local.
- 2026-07-28 · Claude · **FIX: "Pagamento antecipado" do preview/PDF do cliente não incluía a consultoria** (`public/js/app.js` `renderPreview`, só FRONT). Diogo mostrou o "RESUMO FINANCEIRO" client-facing: o "Total a pagar com antecedência" tinha 30% tours + transportes + experiências + itens, mas **faltava a Roteirização e Suporte (consultoria)**. Causa: `const antecipado = sinal + tTr + tEx + tItens;` (sem `cons`). FIX: `+ cons` no antecipado + linha "Roteirização e Suporte — valor integral" no bloco (só se cons>0). Agora antecipado + saldo(70% tours) = total. Ex. do Diogo: 240.940 → **270.940** (+30.000). Bate com a regra da entrada. `node --check` OK; `app.js`→`_06`; zip. Só FRONT → `Ctrl+Shift+R`. (NB: há 2 resumos — o admin `updateResumo` em app-cotacao.js e este client-facing em app.js/renderPreview; ambos agora com a mesma fórmula.)
- 2026-07-28 · Claude · **Revisão Heian×cliente: cotação client-facing agora esconde itens do cliente** (`public/js/app.js` `renderPreview`, só FRONT). A pedido do Diogo (revisão). ACHADO: o total já ignorava `compradoHeian===false`, mas as LINHAS do preview/PDF mapeavam TODOS os transportes/experiências → item "comprado pelo cliente" aparecia como linha ¥0 na cotação. FIX: `transpHeian`/`expHeian` = filter `compradoHeian !== false`, usados nas linhas, totais e na visibilidade das seções → cotação lista SÓ o que é Heian. PORTAL (`cliente.html`) CONFERIDO e OK (não mexi): sub-abas "Documentos & Vouchers Heian" vs "Compras & Reservas do Viajante", badges "Comprado pela Heian/pelo cliente", listas `_transpHeian`/`_expHeian`. `node --check` OK; `app.js`→`_05`; zip. Só FRONT → `Ctrl+Shift+R`.
- 2026-07-28 · Claude · **Regra do 1º pagamento (ENTRADA) corrigida na cotação + timeline** (`server.js` + `app-cotacao.js` + `app-cliente.js`). Diogo: o "sinal" não é só 30% dos tours — o cliente também paga 100% de transportes + experiências + itens adicionais + consultoria (roteirização/suporte), que a Heian desembolsa adiantado. Nova regra: **ENTRADA = 30% dos tours + 100% do resto = total − 70% dos tours**. (1) SERVER: `calcularToursOrcamento` + `calcularSinalOrcamento(orc)` (= total − 70% tours) + `derivarSinalDeLista`; o endpoint `/api/notion/clientes` agora devolve `sinalDevido` por cliente. (2) COTAÇÃO (`updateResumo`): a linha "Sinal 30% — Tours" virou **"Primeiro Pagamento (Entrada)"** = `tT*0.30 + tTr + tEx + tItens + cons`, e adicionei "Saldo (70% dos Tours)". (3) TIMELINE: marco "Sinal 30%" → **"1º pagamento"**, acende com `totalPago >= cliente.sinalDevido` (fallback 30% do total se faltar). `node --check` OK nos 3; `app-cotacao.js`→`_03`, `app-cliente.js`→`_06`; zip. **⚠️ muda server → deploy + reiniciar; testar local.** PENDENTE p/ revisar (Diogo diz que já funciona): (a) cotação client-facing mostrar SÓ o que é Heian; (b) portal separar Heian×cliente em Vouchers/Ingressos.
- 2026-07-28 · Claude · **FIX: mudar status na ficha exigia F5 pra refletir** (`public/js/app-cliente.js`, `mudarStatusClienteFicha`; só FRONT). Causa: `notionClients` (var que a ficha relê em `abrirDetalhesCliente`) e `window.notionClients` (que o kanban usa) podem DIVERGIR — o dashboard às vezes troca só `window.notionClients` por um array novo (`refrescarClientesEmBackground`). Eu atualizava só `window.notionClients`, então a ficha relia a `notionClients` velha → status antigo até o F5. FIX: mutar `.status` **no objeto, em lugar** (não trocar o elemento do array) nas DUAS referências (`notionClients` e `window.notionClients`), e espelhar no local o objeto atualizado. Agora `abrirDetalhesCliente(id)` re-renderiza com o status novo na hora. `node --check` OK; `app-cliente.js`→`_05`; zip. **Só FRONT → `Ctrl+Shift+R`.**
- 2026-07-28 · Claude · **Selo de status da ficha virou SELETOR (mover cliente de etapa direto do selo)** (`public/js/app-cliente.js`, só FRONT). Diogo queria clicar no selo (ex.: "AGUARDANDO TOUR") e escolher pra onde mover. FIX: selo ganhou `cursor:pointer` + caret ▾ + `onclick=abrirSeletorStatusCliente(id, this)`. `abrirSeletorStatusCliente` abre um dropdown posicionado (position:fixed pelo rect do selo) com a lista de status (reusa `window.kanbanStatusesFromNotion`; senão faz fetch em `/api/notion/status-opcoes`; senão fallback embutido), marca o "atual", fecha ao clicar fora. `mudarStatusClienteFicha(id, novo)` faz `PATCH /api/notion/clientes/:id {status}` (MESMO endpoint que o drag do kanban usa — já existia), atualiza o cache `window.notionClients` + espelha no local, re-renderiza a ficha (`abrirDetalhesCliente(id)` → selo+timeline atualizam) e o kanban. Sem mudança de server. `node --check` OK; `app-cliente.js`→`_04`; zip regenerado. **Só FRONT → `Ctrl+Shift+R`** (não precisa reiniciar o `.bat`). **Testar:** abrir ficha, clicar no selo de status, escolher outra etapa, ver o selo/timeline/kanban refletirem.
- 2026-07-28 · Claude · **Novo: TIMELINE do cliente na ficha (6 marcos automáticos)** (`server.js` + `app-cliente.js` + `roteiros.js`). Barra de progresso com bolinhas no topo da ficha (entre header e abas), 6 marcos, TODOS automáticos (decisão do Diogo): **Sinal 30% pago** (totalPago ≥ 30% do valorTotal), **Roteiro pronto** (status "versão final" ou já em viagem), **Enviado ao cliente** (marcador `materialEnviado`, gravado ao clicar Enviar E-mail), **Emissões completas** (todo item compradoHeian tem voucher — calculado do dbRotas), **Saldo quitado** (pago ≥ total), **Em viagem** (data de início chegou ou status). Bolinha feita = cheia vinho c/ check; atual = anel dourado; futura = cinza; + contador "N/6". PEÇAS: (1) `POST /api/clientes/:id/marco` grava marco em `clientes_locais.data.marcos` preservando o resto; (2) `renderTimelineClienteHTML(cliente, vouchers, marcos)` em `app-cliente.js`, injetada no header, param `marcos` passado do fetch `/api/clientes/local/:id` (`d.marcos`); (3) `roteiros.js` guarda `__emailClienteId` ao abrir o modal e faz POST do marco `materialEnviado` no envio OK. **Aditivo/robusto:** `try/catch` retorna '' se algo faltar (não quebra a ficha); o POST do marco não bloqueia o envio. Decisão: "cotação enviada" e "roteiro enviado" fundidos em 1 marco ("Enviado") porque o app tem UMA ação de envio só. `node --check` OK nos 3; `app-cliente.js`→`_03`, `roteiros.js`→`_18`; zip regenerado. **⚠️ muda `server.js` → deploy + reiniciar; testar local:** abrir ficha de cliente (barra aparece), enviar e-mail (acende "Enviado"), conferir os marcos financeiros/emissões. Contexto de design no fim do dia; base era `calcularEstagioCliente` (mas os marcos são calculados direto, independentes).

---

## Estágio atual
- **Sessão de 2026-07-28 (grande):** `app.js` fatiado (9152→~2860 linhas, 6 módulos: eventos/calendario/colaboradores/cliente/cotacao/emails); egress do Supabase otimizado (histórico 5→2 + views `*_light`) + **RLS ligado** em todas as tabelas; **timeline** do cliente na ficha; selo de status virou **seletor**; **quadro dinâmico** do Notion; regra da **ENTRADA** (30% tours + 100% do resto) na cotação+timeline; **anexo automático de PDF** no e-mail; **feedback de save** + fix da corrida do toggle compradoHeian; **agrupamento por cidade** na Base (atrações/rotas/hotéis); e o **sync Base⇄Sheets à prova de erros** (Apps Script v8 + backup/importar/restaurar + **backup diário automático ~4h**). Quase tudo testado AO VIVO via Chrome. `gerar-zip.py` é o build oficial do zip.
- **Em produção (Hostinger):** ainda a versão ANTES desta sessão — **o deploy de tudo de hoje está PENDENTE** (ver Pendências). Deploy = zip manual (extrair sem apagar `.env` → `npm install` → reiniciar Node).
- **Em teste (localhost:3000):** rodando o código de hoje (Diogo reiniciou o `.bat` e validou várias features via Chrome). Deps em dia (`npm audit` = 0).
- **Guia do Japão v4.2 (cliente.html):** 5 capítulos com fotos. Só HTML — não precisa reiniciar o `.bat`.
- **Fonte de verdade do código:** esta pasta (`C:\Users\User\Documents\heian-quote`, com `.git`). O admin roda pelo `server.js` (raiz) e é servido em `/admin` → `public/app.html`.

## Pendências
**Operacional (curto prazo):**
- **★ TAREFA PRO ANTIGRAVITY — completar as FOTOS dos 28 hotéis da base "Indicações de Hotéis".** Diogo delegou. Antigravity JÁ preencheu **estrelas, sites oficiais, Google Maps, bairros e tags** (Log de 29/07) ✓. **Falta: as FOTOS** (até 3 por hotel, na ordem em que aparecem — a 1ª é a capa) e uma **revisão de fatos das descrições**. As fotos que entraram até agora eram genéricas (Unsplash) e não vingaram — precisam ser imagens reais do hotel. No admin, o modal de edição agora tem **3 campos de foto ordenados** (Foto 1/2/3); via API é o mesmo array `fotos`.
  - **Onde os dados moram:** Supabase, config `id='hoteis_indicacoes'` (array). Gerencia por 2 caminhos equivalentes: (a) UI = `/admin` → Base de Dados → aba **"Indicações"** → editar cada hotel; **ou (b) API** (mais rápido em lote): `GET /api/hoteis-indicacoes` lista todos (cada um tem `id`); `PUT /api/hoteis-indicacoes/:id` com JSON dos campos a mudar (**faz merge** — manda só o que for preencher). NÃO usar o sync do Sheets (essa base não entra nele).
  - **Campos (nomes EXATOS):** `nome` · `cidade` · `bairro` · `estrelas` (número 1–5) · `categoria` (Ultra-luxo/Luxo/Boutique/Ryokan/Custo-benefício/Lifestyle-Moderno) · `descricao` · `fotos` (**array de URLs** de imagem; a 1ª é a capa) · `maps` (URL do Google Maps) · `site` (URL do site oficial) · `tags` (array) · `ativo` (bool, manter `true`).
  - **Regras do preenchimento:** (1) `fotos` = **URLs diretas de imagem** que carreguem em `<img>` (terminando em .jpg/.png/.webp; nada de página HTML). 1 a 3 por hotel, boa qualidade, de fonte estável (site oficial/press kit). Sem foto o card mostra só um fundo vinho com a inicial — a foto é o que deixa premium. (2) `maps` = link do Google Maps do endereço certo. (3) `site` = site OFICIAL do hotel. (4) `estrelas` = classificação realista. (5) **Revisar `descricao`**: foram generalizadas de um PDF personalizado (cliente Spilberg) — conferir localização/categoria e corrigir se estiver errado, mantendo o tom consultivo (o que tem de bom + 1 ponto de atenção). Nomes que já corrigi: *The Hotel Seiryu Kyoto Kiyomizu*, *The Gate Hotel Kyoto*, *Park Hyatt Kyoto*.
  - **NÃO** alterar os nomes dos campos nem a estrutura, **NÃO** criar/excluir hotéis (só preencher os 28 existentes), **NÃO** mexer no `server.js`/HTML da página. Conferir ao final abrindo `/indicacoes-hoteis.html` (28 cards, imagens carregando, modal abrindo). Os dados são compartilhados local↔produção (Supabase), então preencher em qualquer um vale pros dois.
- **DEPLOY das sessões de 28-29/07 pra Hostinger (o grande pendente) — TENTADO 29/07, TRAVOU na Hostinger.** O upload no fluxo "Reimplementação → Carregar novos arquivos" deu **"Não foi possível preparar o serviço, tente mais tarde"** (erro de infra da Hostinger, NÃO do zip — íntegro, 4,5 MB / 58 arquivos / 0 barra invertida). **Ação: retentar mais tarde** (ou suporte Hostinger 24/7). Quando voltar: subir e extrair o `heian-tour-DEPLOY.zip` (SEM apagar o `.env`) → `npm install` → **reiniciar o Node** (ativa no server: rotas de Indicações + rota limpa `/indicacoes-hoteis` + whitelist de senha da página, sync do Sheets, `sinalDevido`, backup diário, quadro dinâmico, egress views) → Ctrl+Shift+R → smoke-test (`/indicacoes-hoteis` abre sem senha com 28 cards + logo + clique no card; ficha/timeline+seletor de status; cotação/Primeiro Pagamento; Base/agrupamento+barra de sync; portal regerando o link). **Pré-reqs de Supabase/painel já FEITOS:** RLS, views de egress (`SQL-EGRESS-VIEWS-SUPABASE.sql`), `PORTAL_LINK_SECRET`, Apps Script **v8** implantado + URL salva em `config.sheets_script_url`. **⚠️ Sem esse deploy, a URL curta `/indicacoes-hoteis` dá erro e, se subir SEM o fix de whitelist, o cliente cai no login — usar sempre o zip mais recente.**
- **Limpar a aba de transportes no Sheets** — tem ~325 linhas a mais que o app (lixo/duplicata do sync antigo `appendRow`). O backup sinaliza, não apaga.
- Preencher placeholders do capítulo Emergências do Guia ([seu WhatsApp], seguro, link `wa.me`).
- Instalar a skill `SKILL-heian-roteirista.md` (Settings → Capabilities).
- Apagar as 3 páginas de TESTE no Notion ("TESTE Yamada — apagar"). Opcional: apagar o cliente-exemplo "Família Yamada".

**Features:**
0. **IA de roteiro — parada por bug do Google** (não urgente). Reativa quando: (a) o Google corrigir as chaves "AQ." por REST/SDK, ou (b) entrar uma chave válida — `AIza` (conta sem organização) ou provedor pago (OpenAI/Claude, ~centavos/mês). O código do endpoint já está pronto (SDK `@google/genai`). Se for provedor pago, trocar a chamada por esse provedor. Obs: dependência `@google/genai` adicionada ao package.json — no deploy, garantir que ela suba (node_modules) ou rodar `npm install` no servidor.
1. **Provisões Fase 0** — campos de prazo por item na base (transportes/experiências/atrações: `acao`, `janelaAbreDias`, `prazoDias`, `precisaReserva`).
2. **Provisões Fase 2** — botão "↗ Notion" (empurra tarefa pro Tasks Tracker, com dedup) e Status=Done de volta.
3. **Duplicar roteiro para outro cliente** — botão que copia os dias e religa no cliente certo (nome novo pra não colidir).
4. **Construtor de formulário** (opcional) — tornar `/cadastro` 100% editável pelo App.
5. **Guia por Cidade** (próxima fase combinada c/ Diogo) — base "Cidades" no Sheets (resumo, bairros, especialidades, eventos por estação, link japan.travel) → capítulo "Seus Destinos" no portal filtrado pelas cidades do roteiro do cliente. Claude rascunha as ~10 cidades mais usadas, equipe revisa.
6. **Restaurantes** (fase futura) — base por cidade/bairro no Sheets; sugestões contextuais no dia do roteiro ("Onde comer por perto"); só indicação + link, sem reserva. Fase 2 do Guia: "quero provar" nas comidas, ligado ao Perfil de Viagem.
7. **"Forma de usar" + Maps de embarque por transporte/experiência** (estratégia combinada c/ Diogo em 30/07 — só planejado, nada codado). OBJETIVO: ao marcar um transporte/experiência específico no roteiro, auto-preencher (a) uma **descrição de como usar** aquele serviço (QR no portão / bilhete físico / IC card / retirada no guichê / digital) e (b) um **link de Google Maps do local de embarque**. BOA NOTÍCIA: 80% já plumbed — a base de Transportes já tem campo `uso` (→ `el.instrucoesPosCompra`) e `compra` (→ `instrucoesPreCompra`) que o `selecionarTransporte` copia e o portal já exibe; experiências já têm `localEncontro`. ESTRATÉGIA APROVADA: (1) **híbrido base+override** — a forma de usar mora no CADASTRO do transporte (escreve 1x, auto-preenche no roteiro, editável por viagem); (2) **campo "tipo de uso" como LISTA FECHADA** (QR Code / Bilhete físico / IC Card / Retirada no guichê / Digital-App) **+ descrição livre** — a lista gera selo/ícone no portal e o texto dá os detalhes (evita duplicar cadastros tipo "Shinkansen-QR"); (3) **"local de embarque" nível estação** no cadastro do transporte, com link de Maps explícito OU auto-gerado do nome da estação (mesmo truque dos hotéis); experiência reusa `localEncontro`. EXIBIÇÃO no portal: no card do serviço já existente → selo do tipo de uso → descrição → botão "Como chegar ao embarque" (Maps) → o bilhete (QR/PDF). DECISÕES AINDA ABERTAS p/ Diogo: a forma de usar varia muito por viagem (peso do override)? tipo de uso como lista fechada c/ selo (recomendado) ou só texto? local de embarque fixo por rota (cadastro) ou por viagem? Usar ícones do sprite, não emoji.

## Regras invioláveis (segurança/produto — NÃO furar, o estrago é grande)
- **NÃO quebrar** o que conecta a Notion, Google Sheets, Hostinger ou GitHub.
- **Área do cliente é só por link** — NUNCA pede senha, nem no primeiro acesso.
- **Anexos do chat isolados por cliente** (prefixo `chatIdNorm(clienteId)_`) — nunca vazar entre clientes.
- `.env` fica na Hostinger e **nunca** entra no zip. Em produção, manter `PORTAL_LINK_SECRET` fixo (senão o link do cliente quebra ao trocar a senha do admin) — nunca mudar/remover.

## Regras de trabalho (processo — o Diogo liberou os agentes a "voar" em 2026-07-28)
- **Verificar SEMPRE com `node --check`** o que mexer (isto continua obrigatório — é a rede de segurança que sustenta o resto).
- **Edição de arquivos por tamanho:** abaixo de ~1500 linhas → ferramentas normais (Read/Edit). Só os genuinamente gigantes (`server.js`, e o que ainda restar grande no `app.js`) → via script python (as ferramentas truncam esses). O fatiamento contínuo vai encolhendo essa lista — cada módulo extraído sai do "modo python".
- **Refatorar/emagrecer o core está LIBERADO** quando coberto por rede de segurança: (a) backup/estado reversível, (b) `node --check`, (c) histórico de versões do Supabase. "Aditivo (enhance layer em `try/catch`)" deixa de ser obrigatório e vira só o **default recomendado** pra feature de risco/ao vivo. Mudança de tela segue **cirúrgica e reversível** — mas "cirúrgico" agora pode incluir reescrever, não só empilhar camada. (Foi assim que a Fase 2 e o fatiamento andaram sem susto.)
- **Objetivo declarado do Diogo:** que os agentes voem e entreguem o melhor. Priorizar simplicidade e código enxuto; não ter medo de mexer no core com a rede de segurança acima.

## Como testar / deployar
1. Reiniciar o `.bat` (recarrega o `server.js`) e abrir `localhost:3000`.
2. Testar a mudança (ex.: anexo → reiniciar → baixar de novo prova durabilidade).
3. **Gerar o zip com `python3 gerar-zip.py`** (NÃO montar zip na mão). O script monta os dois nomes (`heian-tour-DEPLOY.zip` e `heian-tour-prod.zip`) com **bytes idênticos**, sempre com barra normal `/`, incluindo `public/` inteiro por varredura (nenhum arquivo novo é esquecido) e **sem** `.env`/`node_modules`/`.zip`; verifica no fim (0 barra invertida + `node --check` em `server.js` e todo `public/js/*.js`). Raiz empacotada: `server.js`, `package*.json`, `README.md`, `Iniciar Heian Tour.bat`, `APPS-SCRIPT-v8.gs`, `ESTADO-DO-PROJETO.md` (ajustável na lista `ROOT_FILES` do script).
4. Subir na Hostinger em lote (os dois zips são iguais → pode subir qualquer um; hábito atual = o `DEPLOY`).

> **Meta de deploy (futuro, melhor retorno):** deploy via git (a pasta já tem `.git`). Se a Hostinger puxar do GitHub, os zips manuais e a classe de bug do zip somem de vez. `gerar-zip.py` é a ponte segura até lá.

## Sync da Base ⇄ Sheets (como usar)
- **Automático (instantâneo):** editar um item na Base pelo app já empurra a mudança pro Sheets na hora (sync per-op, `syncToGoogleSheets`). CONTINUA funcionando. Mas é "dispara-e-esquece" — pode falhar em silêncio.
- **Automático (diário, garantia):** todo dia ~4h o servidor roda o backup completo sozinho (`agendarBackupDiarioSheets` no `server.js`) — mesmo confiável do botão, sem clique. Roda em produção (24/7); local só se o `.bat` estiver ligado.
- **☁️ Backup no Sheets (app→Sheets):** a GARANTIA. Reescreve tudo em lote (upsert por id, preserva fotos/fórmulas, confere contagem). Rodar periodicamente (fim do dia / depois de editar vários itens). Foi o que curou os ids dos hotéis. Avisa se o Sheets tiver linhas a mais (ex.: transportes com 325 duplicatas do sync antigo — limpar na aba).
- **⬇️ Importar do Sheets (Sheets→app):** só quando você editou DIRETO no Sheets (ex.: colou 50 hotéis). Mostra prévia (novos/alterados/removidos/sem-id) antes de aplicar; faz snapshot antes de sobrescrever.
- **↩ Restaurar backup:** desfazer — restaura a base do app de um snapshot (guarda as últimas 3). Não mexe no Sheets.
- Regra simples: editou no app → o Backup garante; editou no Sheets → Importar (com prévia). URL do Apps Script (v8) fica em `config.sheets_script_url` (Supabase, compartilhado local↔prod).

## Mapa rápido
- Backend: `server.js` (raiz). Camadas front: `public/js/*-enhance.js`, `chat-admin.js`, `dashboard.js`, `roteiros.js`, `app.js`. Portal: `public/cliente.html`.
- Integrações: Notion (Clientes, Tasks Tracker), Supabase (`roteiros`, `orcamentos`, `clientes_locais`, `config`, `chat_mensagens`, Storage `chat-anexos`).
- Skills de roteirização: `heian-roteirista` (monta o rascunho do roteiro+cotação), `heian-revisor` (confere antes de enviar ao cliente) e `heian-retroativo` (reproduz no app uma viagem antiga a partir de PDF/Notion).
